# 컨테이너 안에서 실행되는 파일은 Windows 체크아웃에서도 LF 유지 — CRLF 면 /app/start.sh 가 "#!/bin/sh^M" 로 깨져 파드가 죽는다
* text=auto
*.sh text eol=lf
Dockerfile* text eol=lf
*.conf text eol=lf
*.sql text eol=lf
*.yaml text eol=lf
*.png binary
*.mp4 binary
*.zip binary
*.woff2 binary
