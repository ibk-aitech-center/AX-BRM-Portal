# 사용자 지시 외에 지켜야할 준수사항이야 아래 내용들 참고해줘

* 화면은 vue.js, 서버는 springboot로 구성해줘

* 화면은 프로젝트명-ui, 서버는 프로젝트명-api 이런 이름으로 구성해줘

* DB는 postgreSQL 기반으로 구성해줘, 필요하면 DockerHub에서 다운받아서 내 PC에서 postgreSQL이 구동시킨 후 DB를 구성해줘

* postgreSQL의 DB명을 프로젝트 명으로 하고 계정도 프로젝트 명, 그리고 해당 DB에 이 프로젝트에 할당된 계정이 권한을 갖게 해줘

* Dockfile 참고해서 nginx랑 java가 설치된 base 이미지를 만들어줘, 해당 base이미지를 구성 후  프로젝트명-dist 폴더에 tar로 save 해줘

* 첨부파일이나 임시파일은 uploads 폴더에 저장하게끔 지정해줘 해당 폴더는 향후 k8s에서 pvc 볼륨 마운트 할 예정이니 감안해서 적절한 위치에 구성해줘(api에 구성하면 적절할것으로 보여)

# 아래는 사용자 지시 이후에 해야 될 작업들

* 데이터 베이스의 레이아웃을 DB.MD 파일로 작성해줘(서비스에서 DB 미사용시 작성하지 않아도됨)

* DB.MD 파일을 참고해서 dist폴더에 init-db.sql를 구성 해줘 해당 파일은 테이블 생성 및 권한부여 스크립트가 들어가게끔 구성해줘(서비스에서 DB 미사용시 작성하지 않아도됨)

* init-db.sql에 postgreSQL 계정이 프로젝트 DB에 접근권한을 가지게 하는 명령어를 맨 아래줄부터 넣어줘(서비스에서 DB 미사용시 작성하지 않아도됨)

* 최종 작업 후 운영계 배포를 위해 pdf-agent-dist 폴더에 배포를 위한 파일들을 업데이트 하는데
 - ui의 경우 build를 다시 해서 배포된 걸 ui.tar.gz로 압축해서 넣어줘
 - api의 경우 app.jar로 말아서 넣어줘
 - 그리고 ui.tar.gz랑 app.jar 파일을 dist 폴더에 최종 dist.zip 로 압축해줘
