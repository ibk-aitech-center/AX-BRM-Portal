# 디자인 & UI/UX 가이드라인

> **AI&Tech센터 사내 서비스 표준 디자인 지침**
> 14개 프로젝트(condo_picker, the-fans, pdf-agent, image-agent, recommend-agent-poc, corporate_auction_loan, corporate_loan_advisor, baby-calender, ai-boomup-festa, mini-HRS, must-go-restaurant, prize-calculator, promo, AI Agent 경진대회 보고)의 실제 구현 코드를 전수 분석하여, 가장 완성도 높은 프로젝트의 패턴을 표준으로 채택했습니다.
>
> **평가 축을 분리합니다.** "디자인 시스템이 정교한가"(시각 완성도)와 "실제 사용자가 실제 업무를 완수하기 좋은가"(업무 UX)는 다른 축이며, **실무 서비스 개발 시 우선 기준은 업무 UX**입니다.

### 역할별 표준 레퍼런스

| 역할 | 표준 프로젝트 | 참조할 것 |
|---|---|---|
| **업무 서비스 UX 표준** ★최우선 | `corporate_loan_advisor` | 인지 부하 설계(아코디언 단계 노출), 진행 트리거 계약, 경고 비침투, 코칭 페르소나, 실패 상태의 정직함. 복잡한 도메인 업무를 초심자도 완수하게 만드는 설계 전반 |
| **생산 견고함 표준** | `pdf-agent` | 반응형(360px까지), 도구 간 동일 화면 골격(학습 비용 제로), 도구 간 파일 이어쓰기, 접근성, 에러/진행 상태 카탈로그. 실배포 서비스의 완성 기준 |
| **복잡 업무 폼 레퍼런스** | `corporate_auction_loan` | 서브스텝 아코디언 상태 머신, 시맨틱 컬러 페어, A4 인쇄 리포트 분리 |
| **토큰 아키텍처 레퍼런스** | `the-fans/index.html` | 간격/라운드/그림자/이징 토큰 스케일의 구조. **주의: 클릭 데모용 목업이며 실사용 UX는 검증되지 않음 — 시각 시스템만 참조하고 UX 패턴의 근거로 삼지 말 것** |
| **프레젠테이션 표준 — 보고형** (정보 전달) | `AI Agent 경진대회 보고 덱` | "1슬라이드 1메시지" 3원칙, 네이비×골드 팔레트, CSS 데이터 시각화(도넛/퍼널/드릴다운 대시보드). 임원 보고·결과 보고용 |
| **프레젠테이션 표준 — 피치형** (설득·경연) | `corporate_loan_advisor/05_ppt/ppt_최종통합본_v2.html` | 서사-무대 융합(날씨가 곧 논증 구조), 누적 환경 레이어, 타이포 사투리 전환, 라이브 데모 iframe 게이팅, 클릭 단계형 카운터, 대본 내장. 경연·투자 피치처럼 감정을 움직여야 하는 발표용 |
| **히어로/쇼케이스 연출 레퍼런스** (외부) | `beam.jakubantalik.com` · `orbs.jakubantalik.com` | 보더 빔(conic-gradient+mask), 점묘 오브(canvas 2D), 시머 텍스트, blur 크로스페이드 — §7 후킹 연출 레시피의 원전. **주의: 데모용 쇼케이스 — 연출 계층에만 참조하고 시스템 표준(토큰·컴포넌트·성능 규칙)의 근거로 삼지 말 것** |
| **브랜드 메타포 무대화 레퍼런스** (외부) | `igloo.inc` | 회사명(Igloo)을 빙산 메타포로 무대화 — 스크롤=빙하 하강 여정, 포트폴리오 회사를 얼음 블록에 봉인해 "하나의 빙산 아래 여러 벤처"라는 지주회사 구조 자체를 시각 논증으로 수행. §1-③ 메타포 확장·§9 서사-무대 융합의 최상급 예시. **주의: 개념(메타포=논증 구조, 스크롤=여정)만 참조 — 기술 스택(WebGL/Three.js)은 표준 밖(§7 명시적 비표준)** |

### 목차 & 사용 순서

**새 프로젝트를 시작한다면 §14(킥오프 인터뷰) → §12(체크리스트) 순서로 읽으세요.** 나머지는 작업 중 참조용입니다.

| 계획 단계 | 설계 표준 | 구현 표준 | 부록 |
|---|---|---|---|
| §14 킥오프 인터뷰 | §1 디자인 철학 9원칙 | §5 컴포넌트 패턴 | §11 안티패턴 |
| §12 시작 체크리스트 | §2 표준 토큰 | §6 상태 & 피드백 | 개정 이력 |
| §13 DESIGN.md 운영 | §3 타이포그래피 | §7 모션 규칙 | |
| §17 사용성 검증 루프 | §4 레이아웃 셸 | §8 접근성 8종 | |
| | §9 프레젠테이션 | §15 구현 규율(CSS·성능) | |
| | §10 인쇄물 | §16 사용자 만족 패턴 ★대원칙 | |

---

## 1. 디자인 철학 — 지금까지의 작업에서 도출된 9가지 원칙

모든 프로젝트를 관통하는 일관된 제작 철학이 확인되었습니다. 향후 개발에서도 이 원칙을 유지합니다.

### ① 검증된 디자인 시스템의 토큰 단위 이식 (차용의 원칙)
바닥부터 발명하지 않는다. 국내 사용자에게 이미 학습된 최고 수준의 디자인 언어를 **CSS 변수 토큰 단위로 이식**한다.
- 금융/업무 도구 → **Toss** (`#3182F6`, ink 뉴트럴 램프) — mini-HRS, recommend-agent, the-fans
- 소비자형/마켓플레이스 → **Airbnb** (`#ff385c`, 단일 강조색 + 단일 그림자) — condo_picker
- 생활/친근함 → **배민** (`#2AC1BC` 민트) — baby-calender
- 차용하되 반드시 `:root` 토큰 블록으로 체계화하여 이식한다. 색상 하드코딩으로 흉내내지 않는다.
- **차용 소스 카탈로그**: [github.com/VoltAgent/awesome-design-md](https://github.com/VoltAgent/awesome-design-md) — 유명 서비스 ~73개의 디자인 시스템을 DESIGN.md(마크다운 스펙)로 추출해둔 컬렉션 (Airbnb, Stripe, Linear, Apple 등). 해외 시스템을 차용할 때는 여기서 해당 DESIGN.md를 가져와 프로젝트 루트에 두고 시작한다 — 기억에 의존한 역설계 대신 검증된 스펙에서 출발 (condo_picker가 Airbnb DESIGN.md로 검증한 방식). Toss/배민 등 국내 시스템은 카탈로그에 없으므로 기존처럼 직접 이식.

### ② 토큰 우선 (Token-First)
모든 프로젝트는 코드 첫머리에 `:root` 디자인 토큰 블록을 선언한다. **색상·간격·라운드·그림자·모션 곡선·z-index까지 전부 토큰**이며, 컴포넌트 CSS는 토큰만 참조한다. (가장 흔한 실패 패턴이 "토큰 선언 후 하드코딩" — §11 안티패턴 참고)

### ③ 맥락에 맞는 감성 (Context-Matched Tone)
하나의 브랜드라도 **맥락에 따라 팔레트를 의도적으로 분리**한다. 이것은 일관성 위반이 아니라 설계다.
- 제품 UI: 밝은 Toss 블루 (신뢰 + 친근)
- 임원 보고 덱: 다크 네이비 × 골드 (무게감 + 프리미엄)
- 이벤트/마케팅: 네온 온 블랙 (에너지)
- 출산 축하 포스터: 스크랩북/색종이 콜라주 (다정함)
- 인쇄 공문: 네이비 + 인장 레드 (격식)
- 제품 쇼케이스/데모 랜딩: 미니멀 크래프트 다크 — `#070707` 배경 + 흰 알파 레이어링 서피스 + 단일 이징 (§7 후킹 연출 레시피와 페어. 한 번 보는 첫인상용 맥락이며 업무 화면 팔레트가 아님)
- **브랜드 메타포를 시각 시스템 전체로 확장**할 수 있다: corporate_loan_advisor("대체로 맑음")는 날씨 메타포를 팀명 → 태양 마스코트 → 구름 걷히는 히어로 안무 → "오늘도 맑음" 카피 → PPT 배경까지 일관되게 관철. 두려운 업무(초심자의 첫 여신 상담)를 "맑아지는 하늘"로 재프레이밍한 감성 설계.
- 팔레트 차용은 출발점일 뿐 고정이 아니다: Toss 블루로 시작해 서비스 정체성에 맞춰 틸 그린(`#0D9488`)으로 교체한 전례(corporate_loan_advisor, 문서화된 의사결정). 단, 고객 전달물(PDF)에는 별도 예약 색(IBK 네이비 `#003594`)만 사용.

### ④ 폐쇄망 우선 (Closed-Network First)
행내망(망분리) 배포를 기본 전제로 한다.
- 폰트는 **로컬 번들** (Pretendard woff2 self-host), 외부 CDN 의존 최소화
- 일러스트·아이콘·차트는 **순수 CSS/인라인 SVG**로 제작 (이미지 에셋 없이 마스코트·도넛차트·간트·야경 일러스트까지 구현한 전례 있음)
- 오프라인 대비: Service Worker, localStorage 영속화

### ⑤ 상태의 완전성 (Complete State Coverage)
"행복한 경로"만 디자인하지 않는다. **빈 상태 / 로딩 / 진행 / 성공 / 에러 / 오프라인** 전 상태를 출시 전에 설계한다. 에러는 상태코드를 **사용자가 취할 행동**으로 번역한다 (예: 429 → "이용자가 많아요. 30초쯤 뒤에 다시 시도해 주세요").

### ⑥ 절제된 모션 (Restrained Motion)
모션은 풍부하되 규율이 있다: 이름 붙은 이징 토큰 2~3개만 사용, 진입 애니메이션은 스태거(순차 지연), **`prefers-reduced-motion` 가드 필수**. "1슬라이드 1메시지, 절제된 강조, 충분한 여백" (경진대회 덱의 자체 선언 3원칙).

### ⑦ 한국어 타이포그래피 규율 (Korean Typography Discipline)
Pretendard 기본 + `word-break: keep-all` + 숫자에 `tabular-nums`는 전 프로젝트 공통 규칙. 유동 타이포는 `clamp()`.

### ⑧ 페르소나와 보이스 (Persona & Voice)
사내 도구라도 **말투와 페르소나를 명시적으로 정의**한다 (corporate_loan_advisor에서 확립).
- 페르소나: "옆자리 베테랑 사수" — 지시가 아니라 도움, 코칭하듯.
- 보이스: 친근한 존댓말 ~요체, 짧고 명확하게. ("아직 확인할 유의사항이 없어요 🙂")
- 이모지는 절제하되 **역할을 고정**한다: 💡 일반 팁 · ✨ 긍정/기회 · ⚠️/🚨 경고 · 💬 코칭 · 🖥️ 외부 시스템 작업.
- 심각도는 **색 + 이모지 + 텍스트 3중 부호화** — 색상만으로 의미를 전달하지 않는다.
- **마이크로카피 규칙**: 버튼은 결과가 보이는 동사로 — "확인"(✕) 대신 "추천 받기"·"계산하기"·"저장하고 다음"(○). 제목·라벨은 시스템 용어가 아니라 사용자의 언어로 ("여신 프로세스 개시" ✕ → "상담 시작하기" ○). 빈 상태·에러 메시지에는 **반드시 다음 행동**을 함께 제시한다 (§6과 일관).

### ⑨ 인터랙션 계약의 문서화 (Documented Interaction Contracts)
"느낌으로" 만들지 않고 상호작용 규칙을 명세로 적는다 (corporate_loan_advisor `cf_design_system.md`가 모범).
- 입력 타입별 진행 트리거, 접힘 시 요약 포맷, 필수/선택 무결성 규칙을 표로 명세 → 버그 클래스 자체를 제거.
- 애니메이션은 이름 붙은 타이밍 상수로 명세 (§7).
- 디자인 시스템 문서는 **다른 서비스에 복제(클로닝) 가능한 플레이북**으로 작성한다.

---

## 2. 표준 디자인 토큰 (Canonical Token Set)

**the-fans의 토큰 아키텍처**(토큰 구조 기준으로 가장 체계적 — 단, 시각 시스템 참조용)를 골격으로, recommend-agent의 Toss 램프와 pdf-agent의 시맨틱 세트를 병합한 표준입니다. 새 프로젝트는 이 블록을 복사해 시작하세요. (업무 서비스라면 corporate_loan_advisor처럼 브랜드 색을 서비스 정체성에 맞게 교체 — §1-③)

```css
:root {
  /* ── 브랜드 (Toss 기반 — 프로젝트 성격에 따라 §1-③ 팔레트로 교체) ── */
  --brand-50:  #EAF3FF;
  --brand-100: #D2E6FF;
  --brand-300: #7CB1FF;
  --brand-500: #3182F6;   /* 기본 강조색 — 화면당 1~2회만 */
  --brand-600: #1B64DA;   /* hover/active */
  --brand-700: #1448A6;

  /* ── 뉴트럴 (ink 램프) ── */
  --ink-900: #191F28;     /* 제목 */
  --ink-700: #4E5968;     /* 본문 */
  --ink-500: #8B95A1;     /* 보조 */
  --ink-300: #D1D6DB;     /* 비활성 */
  --ink-200: #E5E8EB;     /* 헤어라인/보더 */
  --ink-100: #F2F4F6;     /* 서피스 소프트 */
  --ink-50:  #F9FAFB;     /* 페이지 배경 */

  /* ── 시맨틱: 반드시 배경+본색 페어로 선언 (뱃지/판정/토스트용)
     네이밍은 success/warning/danger/info로 통일 — 표준 프로젝트(corporate_loan_advisor)
     및 Tailwind 관례와 일치. positive/critical 등 다른 이름 금지 ── */
  --success: #1AB76F;  --success-bg: #E8FAF1;
  --warning: #F59F0A;  --warning-bg: #FFF7E6;
  --danger:  #F04452;  --danger-bg:  #FFEBEE;
  --info:    #3182F6;  --info-bg:    #EAF3FF;

  /* ── 간격 (4px 베이스) ── */
  --space-xs: 4px;  --space-sm: 8px;  --space-md: 16px;
  --space-lg: 24px; --space-xl: 32px; --space-xxl: 48px;

  /* ── 라운드 ── */
  --radius-sm: 10px;      /* 입력/버튼 */
  --radius-md: 16px;      /* 카드 */
  --radius-lg: 24px;      /* 모달/히어로 카드 */
  --radius-full: 9999px;  /* 칩/필 */

  /* ── 그림자 (4단 엘리베이션 램프 — 저알파 레이어드) ── */
  --shadow-sm:       0 1px 4px rgba(0,0,0,0.04);
  --shadow-card:     0 2px 12px rgba(0,0,0,0.06);
  --shadow-elevated: 0 4px 20px rgba(0,0,0,0.08);
  --shadow-modal:    0 20px 60px rgba(0,0,0,0.30);
  --shadow-brand:    0 4px 16px rgba(49,130,246,0.25);  /* 선택/CTA 강조용 */

  /* ── 모션 (이름 붙은 곡선 2종만 — 임의 곡선 금지) ── */
  --ease-out:    cubic-bezier(0.22, 1, 0.36, 1);   /* 기본 진입/전환 */
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1); /* 탄성 강조 */
  /* 시간+곡선만 토큰화 — `transition: all` 금지(§15), 대상 속성은 사용처에서 명시:
     예) transition: transform var(--transition), opacity var(--transition); */
  --transition-fast: 0.15s ease;
  --transition:      0.3s var(--ease-out);

  /* ── z-index (이름 붙은 스케일 — 매직넘버 금지) ── */
  --z-nav: 100; --z-panel: 150; --z-modal: 200; --z-toast: 300; --z-loading: 400;

  /* ── 폰트 ── */
  --font: "Pretendard Variable", Pretendard, "Apple SD Gothic Neo",
          "Malgun Gothic", system-ui, sans-serif;
  --font-mono: "D2Coding", "JetBrains Mono", ui-monospace, monospace;
}
```

**규칙**
- 토큰 블록은 **공유 CSS 파일 하나**에만 선언한다. 페이지마다 복붙 금지 (condo_picker의 3중 복붙이 반면교사).
- React + Tailwind 프로젝트는 같은 값을 `tailwind.config.js` theme으로 미러링한다 (recommend-agent 방식).
- 강조색(`--brand-500`)은 화면당 1~2회만. "흰 캔버스 90% + 강조 1회"가 기본 리듬 (condo_picker의 Airbnb 원칙).
- **색은 역할로 서술한다** (awesome-design-md의 좋은 문서들이 쓰는 방식): 프로젝트 DESIGN.md(§13)에서 색을 hex 나열이 아니라 **어디에 쓰고, 어디에 안 쓰는지**까지 산문으로 적는다.
  - 좋은 예: "Rausch는 단일 브랜드 색. Reserve CTA, 검색 오브, 하트 저장 상태에만 사용" / "별점 숫자는 골드가 아니라 잉크색 — 의도된 절제" / "IBK 네이비는 고객 전달 PDF 전용, 앱 화면에는 등장하지 않음"
  - 나쁜 예: "`--brand-500: #3182F6` (파랑)" — 역할 없이 이름만 있는 토큰은 결국 아무 데나 쓰인다.

**다크 모드 대비 — 역할 토큰 레이어**
다크 테마가 예정된(또는 가능성 있는) 프로젝트는 컴포넌트가 팔레트 토큰을 직접 참조하지 않고 **역할 토큰 한 겹**을 거친다:
```css
:root {
  --bg: var(--ink-50);  --surface: #FFFFFF;  --line: var(--ink-200);
  --text: var(--ink-900);  --text-sub: var(--ink-700);  --text-muted: var(--ink-500);
}
[data-theme="dark"] {   /* image-agent 다크 팔레트를 다크 기본값으로 */
  --bg: #14161B;  --surface: #1C1F26;  --line: #2B303C;
  --text: #E9ECF4;  --text-sub: #C4D0DE;  --text-muted: #8D94A8;
}
```
컴포넌트는 `--surface`/`--text`만 참조 → 다크 전환이 역할 재매핑 한 곳에서 끝난다. 라이트 전용 프로젝트는 이 레이어를 생략해도 되지만, 그 결정을 DESIGN.md Known Gaps에 "다크 모드 미정의"로 명시한다 (§13).

---

## 3. 타이포그래피

| 용도 | 크기/무게 | 비고 |
|---|---|---|
| 히어로/페이지 제목 | 24~28px / 700 | 덱은 `clamp(2.4rem, 5vw, 4rem)` / 900 |
| 섹션 제목 | 18~21px / 700 | |
| 카드 제목 | 16px / 600 | |
| 본문 | 14~15px / 400, line-height 1.6 | |
| 캡션/보조 | 12~13px / 400, `--ink-500` | |
| 대형 수치 (KPI/점수) | 32~36px / 700~800 | `tabular-nums` 필수 |

**필수 전역 규칙**
```css
body { font-family: var(--font); line-height: 1.6; letter-spacing: -0.01em; }
* { word-break: keep-all; overflow-wrap: break-word; }  /* 반드시 페어로 — keep-all 단독은
                                                           긴 URL/영문이 컨테이너를 뚫는다 */
.num { font-variant-numeric: tabular-nums; }      /* 금액·점수·순위 등 모든 수치 */
.identifier { font-family: var(--font-mono); word-break: break-all; }  /* 사업자번호·코드 등
                                                     식별자는 keep-all 예외 — 아무 데서나 끊김 허용 */
h1, h2 { letter-spacing: -0.03em; text-wrap: balance; }  /* balance는 미지원 브라우저에서
                                                            무해하게 무시됨 (장식적 사용만, §15) */
```
- 제목은 700 이상을 아껴 쓴다 — 굵기 남발 대신 크기·색·여백으로 위계를 만든다.
- 프레젠테이션·반응형 화면의 제목은 `clamp(min, vw, max)` 유동 타이포 기본.
- 특수 기법: 인쇄물에서 한자만 명조/해서체로 렌더링하려면 `@font-face` + `unicode-range: U+3400-9FFF` (promo의 검증된 기법).

---

## 4. 레이아웃 패턴

분석된 프로젝트에서 반복 검증된 4가지 표준 셸:

**A. 도구형 앱 셸** (pdf-agent, mini-HRS, must-go)
고정 사이드바(260~320px) 또는 중앙 단일 컬럼(`max-width: 880px`) + 일관된 페이지 골격: `상단 내비 → 업로드/입력 존 → 설정 → 진행 → 결과 카드`. 모든 도구 페이지가 같은 골격을 따르면 도구가 늘어도 학습 비용이 없다.

**B. 에디터 셸** (image-agent)
상단바 / 좌측 도구 레일 / 중앙 스테이지 / 우측 옵션 패널. 옵션 패널은 **스크롤 본문 + 하단 고정 저장 버튼** 2존 구조 — 설정이 늘어도 저장 버튼이 묻히지 않는다.

**C. 폰 프레임 데모 셸** (the-fans)
모바일 서비스 시연은 480px 고정 폰 프레임(sticky 헤더 + 하단 탭바)을 데스크톱에서 둥근 디바이스 목업으로 띄운다. 빔프로젝터 시연용 `@media (min-width:1024px)` 확대까지 고려.

**D. 스크롤 스냅 덱** (프레젠테이션 전 프로젝트)
풀뷰포트 `scroll-snap-type: y mandatory` 슬라이드 + "스크롤리텔링 크롬 킷":
IntersectionObserver 스태거 리빌 + 우측 페이지 도트(활성 도트는 필로 늘어남) + 진행바 + 키보드 내비(화살표/스페이스) + 자동 소멸 스크롤 가이드 + 스크롤 시 블러 생기는 sticky 내비.

**공통 규칙**: 콘텐츠 최대폭 1280px, 카드 그리드는 `repeat(auto-fit, minmax(300px, 1fr))`, 대표 항목은 `grid-column: 1 / -1` 풀폭 히어로 카드로 승격.

---

## 5. 핵심 컴포넌트 패턴

전 프로젝트에서 반복 검증된 컴포넌트 레시피:

**카드**: 흰 서피스 + `--radius-md` + `--shadow-card`, hover 시 `translateY(-2px)` + `--shadow-elevated`. 마케팅 카드만 `-8px` + 컬러 그림자 허용.

**모달**: 그라디언트 히어로 헤더(`linear-gradient(135deg, var(--brand-500), var(--brand-600))`, sticky) + 스크롤 본문 + sticky 푸터 액션. 5개 프로젝트에서 동일 패턴 검증됨. `Escape` 닫기 필수.

**선택형 카드 (radio-as-card)**: 숨긴 input + `:checked` 시 브랜드 그라디언트 채움 + 텍스트 흰색 반전 + `scale(1.01)` (baby-calender 시나리오 카드, must-go 거리 칩).

**칩/필**: `--radius-full`, 선택 시 잉크색 채움. 복수 선택 그룹은 그룹 레이블로 묶는다 (condo_picker 컨셉 칩).

**스테퍼**: ① 상단 도트 스테퍼(default/active/done + 연결바) — 마법사형 흐름용. ② 수직 스테퍼(대기/스피너/체크 + 완료 시 초록 커넥터) — 다단계 비동기 작업용 (pdf-agent OCR).

**서브스텝 아코디언 상태 머신**: `active / done / done.warn / pending` 4상태, 완료 시 자동 접힘 + 요약 한 줄 표시 (corporate_auction_loan). 긴 업무 폼의 표준.

**단계적 노출 셀렉트**: 시/도 → 시/군/구 → 읍/면/동처럼 종속 드롭다운은 `disabled` 게이팅으로 순차 활성화.

**커스텀 날짜 입력**: 마스킹 텍스트 + 달력 아이콘 버튼 + `opacity:0` 네이티브 picker 오버레이 — 크로스브라우저 일관 UX (baby-calender).

**포커스 링**: 입력 `:focus` 시 `box-shadow: 0 0 0 3px rgba(49,130,246,0.18)`, 전역 `:focus-visible { outline: 2px solid var(--brand-500) }`.

**기능별 액센트 테마**: 도구/카테고리마다 고유 색 페어를 토큰으로 부여하고 클래스 접미사(`icon-{tool}`, `card-{tool}`)로 구동 — 기능이 늘어도 자동 확장 (pdf-agent 11개 도구, boomup-festa 4개 프로그램).

**아코디언 질문 플로우** (corporate_loan_advisor): 한 번에 한 질문만 펼침(active), 답변 시 자동으로 다음 미답변 질문으로 진행 + 현재 질문은 `done`으로 접혀 **우측 정렬 볼드 요약 + 연필 수정 버튼**만 남김. 긴 인터뷰형 폼의 표준.

**진행 트리거 매트릭스** (corporate_loan_advisor §9-1): 입력 타입별로 진행 시점을 계약으로 고정 — `select`/`yn`은 클릭 즉시, `text`/`number`는 **Enter 또는 "확인 →" 버튼**, `currency`/`date`/`multiselect`는 버튼. **blur로는 절대 진행하지 않는다** (의도치 않은 조기 진행 방지).

**시그널 카드 + 플라이트 안무** (corporate_loan_advisor): 규칙 발동(경고/기회) 시 카드가 ① 화면 중앙에 스포트라이트로 떠올라 ~1초 머문 뒤 ② 사이드바의 **사전 측정된 예약 슬롯**으로 비행해 도킹. 예약 슬롯은 실제 카드 높이를 먼저 측정하고 점선 펄스 플레이스홀더로 자리를 잡아 **레이아웃 점프를 제거**. 등장/해소는 답변 확정 시점에만 동기화해 타이핑 중 사이드바가 흔들리지 않게 한다.

**경고 비침투 원칙** (corporate_loan_advisor): 경고/주의 메시지는 질문 흐름 중간에 끼워 넣지 않는다. 반드시 우측 사이드바(별도 랙)에만 표시 — 사용자의 진행 리듬을 끊지 않는다.

**누적 코칭 팁**: 팁은 일회성 툴팁으로 사라지지 않고, 접힌 답변 행에 💬 코멘트로 남아 **상담 전체에 걸쳐 누적**된다 — "베테랑 사수의 여백 메모" 모델.

**데이터 테이블**: 숫자 컬럼은 우측 정렬 + `tabular-nums`, 텍스트는 좌측 정렬. 긴 목록은 sticky 헤더(`position: sticky; top: 0`). 빈 값은 공백이 아니라 "–"로 표기. 행 hover 배경(`--ink-50`)으로 스캔 보조. 좁은 화면에서는 `overflow-x: auto` 컨테이너로 감싸거나 카드형으로 변환 — 테이블 자체를 찌그러뜨리지 않는다.

**아이콘 시스템**: 폐쇄망 원칙(§1-④)에 따라 아이콘 CDN 금지 — 인라인 SVG 스프라이트(`<symbol>`) 또는 로컬 번들(lucide)만 사용. stroke 굵기 1.5~2px 통일, 크기는 16/20/24px 그리드, 착색은 `currentColor`로 텍스트 색을 따라가게. 이모지는 아이콘의 대체재가 아니라 §1-⑧ 역할표 안에서만 사용한다.

---

## 6. 상태 & 피드백 카탈로그 (전 상태 필수 설계)

출시 전 아래 6개 상태가 모두 디자인되어야 한다:

| 상태 | 표준 패턴 | 출처 |
|---|---|---|
| **빈 상태** | 아이콘/이모지 + 제목 + 힌트 + **원클릭 퀵 프리셋 버튼** (빈 화면을 시작점으로 전환) | condo_picker |
| **로딩** | 브랜드 마스코트/애니메이션 + 경과 시간 카운터 + **취소 버튼**(AbortController). 오버레이는 **범용 컴포넌트 하나**로 만들어 LLM 대기·섹션 전환 등에 재사용 (`role="status"` `aria-live`). AI 대기 등 연출이 어울리는 맥락은 §7 후킹 연출 레시피의 오브·시머 사용 가능 | image-agent, corporate_loan_advisor |
| **진행** | 시머 진행바 또는 수직 스테퍼. 30초 이상 걸리면 사전 고지 | pdf-agent, must-go |
| **성공** | 결과 카드 `scrollIntoView` 자동 스크롤 + **"다음으로 무엇을 할 수 있나요?"** 후속 흐름 제안. 축하 맥락엔 SVG 체크마크 드로우 애니메이션 | pdf-agent, prize-calculator |
| **에러** | 토스트(성공/경고/에러 좌측 보더 변형, `role="alert"` `aria-live`) + **상태코드→행동 번역** 헬퍼. `alert()` 금지, 인라인 시맨틱 에러 사용. AI 기능 실패 시 **몰래 폴백하지 않는다** — 에러 코드를 명시하고 재시도/설정 안내 제공 | image-agent, condo_picker, corporate_loan_advisor |
| **오프라인** | `window offline` 감지 배너 + localStorage 폴백 | must-go |

**에러 메시지 번역 표준** (image-agent `serverErrorMessage` 패턴):
```
429 → "이용자가 많아 잠시 대기 중이에요. 30초쯤 뒤에 다시 시도해 주세요."
413 → "파일이 너무 커요. 20MB 이하로 줄여서 올려주세요."
5xx → "서버가 잠시 아파요. 잠시 후 다시 시도해 주세요."
```
기술 용어(타임아웃, 게이트웨이)가 아니라 **사용자가 할 행동**을 말한다.

**첫 사용 경험 — "첫 성공까지 3분"을 설계 목표로**: 처음 온 사용자가 도움 없이 첫 결과물을 얻기까지의 시간을 측정 가능한 목표로 잡는다 (§17 복도 테스트의 핵심 관찰 항목). 웰컴 가이드는 짧게 한 번만(must-go 웰컴 모달), 긴 툴팁 투어는 지양 — 대신 빈 상태의 **퀵 프리셋 버튼**(위 표)이 첫 성공으로 가는 최단 경로다. 도움말은 흐름 밖 별도 페이지가 아니라 맥락 안에(§5 누적 코칭 팁, ⓘ 버튼 기준 모달).

**입력 검증 계약**:
1. **타이밍** — 제출/진행 시점에 일괄 검증하고 **첫 에러 필드로 포커스 이동**. 실시간 검증은 형식(숫자만, 자릿수)에만. blur 시점 검증은 "입력 중 질책" 느낌을 주므로 지양 (blur 진행 금지 §5와 일관).
2. **표시** — 에러 필드는 빨간 보더 + shake 키프레임 + 필드 바로 아래 인라인 메시지(`aria-describedby`로 연결). 필수값 누락은 토스트 경고.
3. **label은 항상 표시** — placeholder를 label 대용으로 쓰지 않는다 (입력을 시작하면 사라져 맥락이 소실).
4. **단위 통일** — 금액 등 수치는 입력 단위를 하나로 고정하고("백만원") 저장값과 표시값을 분리 (corporate_loan_advisor CurrencyInput: 원 단위 저장, 백만원 표시).

---

## 7. 모션 규칙

- **이징은 토큰 2종만**: `--ease-out`(기본), `--ease-spring`(탄성 강조). 임의 곡선 금지.
- **진입 리빌**: `opacity 0→1` + `translateY(30~50px)`, 자식 요소는 `nth-child` 스태거 지연 (0.08~0.15s 간격).
- **호버**: 카드 `translateY(-2px)`, 버튼 `translateY(-1px)` / active `scale(0.98)`. CTA 화살표 `translateX(3px)`.
- **수치 연출**: 핵심 KPI는 스크롤 트리거 카운트업 (the-fans 프레젠테이션의 ₩600억 카운트업).
- **필수 가드** — 제거(`none`)가 아니라 **단축**한다. `animation: none`은 `animationend`/`transitionend`에 의존하는 JS 로직(플라이트 도킹, 카운터 완료 등)을 영원히 미완료 상태로 깨뜨린다. 단축하면 로직은 즉시 완료되고 움직임만 사라진다:
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```
- 장식 모션(부유 오브젝트, 시머, 글로우)은 히어로/랜딩에만. 업무 화면에서는 상태 전달 모션만.

**후킹 연출 레시피 — 위 "히어로/랜딩에만" 허용 구역 전용 카탈로그** (원전: beam/orbs.jakubantalik.com 분석, 레퍼런스 표 참조)

첫인상에 투자하는 화면(§14 Q3-③ "한 번 보는 페이지", §14 Q4-⑦ 쇼케이스)에서만 쓰는 검증된 연출 3종. 시스템 표준이 아니라 **격리된 장식 계층**이다.

*공통 사용 조건 (전 레시피 필수)*:
- 화면당 연출 1회만. 매일 쓰는 업무 화면(§16)에는 사용 금지.
- 폰트·에셋은 로컬 번들(§1-④) — 원전의 Google Fonts CDN(Inter/Roboto Mono)을 그대로 가져오지 않는다. Pretendard로 치환.
- 이 계층에 한해 **소형 요소(≤48px 아이콘 등)의 `filter: blur()` 전환 허용** — §15 본칙(transform/opacity 전용)의 유일한 예외. 대면적 blur 전환은 여전히 금지. 대표 활용이 아이콘 스왑 크로스페이드: 사라질 때 `opacity:0; transform:scale(.25); filter:blur(4px)` ↔ 나타날 때 복원, `0.3s var(--ease-out)` (원전 이징이 가이드 `--ease-out`과 동일 곡선 — 추가 토큰 불필요).
- `width` 전환은 이 계층에서도 금지(§15) — 원전의 슬라이딩 필·툴팁 너비 애니메이션을 가져올 때는 `transform: translateX/scaleX`로 변형해서 쓴다.
- **인트로 연출(첫 진입 시퀀스)에는 스킵 수단 필수** — 클릭/`Esc`/스킵 버튼 중 하나 이상 + 재방문 시 자동 생략(localStorage 플래그). 첫인상용 페이지에만 허용하며, "시연 최적화 ≠ 일상 사용" 원칙(아래)의 인트로 버전이다 (igloo.inc·bruno-simon.com의 실시간 인트로에서 검토·격리 편입).

*레시피 ① 보더 빔* — CTA/히어로 카드 테두리를 도는 빛. WebGL·canvas 불필요, 순수 CSS:
```css
/* @property로 각도를 타입 등록해야 conic-gradient 각도가 애니메이션된다 */
@property --beam-angle { syntax: "<angle>"; initial-value: 0deg; inherits: false; }
.hero-card { position: relative; }
.hero-card::after {
  content: ""; position: absolute; inset: 0; padding: 2px;   /* 빔 두께 */
  border-radius: inherit; pointer-events: none;
  background: conic-gradient(from var(--beam-angle),
    transparent 0% 54%, rgba(255,255,255,.3) 60%,
    rgba(255,255,255,.75) 66%, rgba(255,255,255,.3) 72%, transparent 78%);
  /* 패딩 안팎을 XOR — 테두리 링만 남기는 마스크 합성이 핵심 */
  mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask-composite: exclude; -webkit-mask-composite: xor;
  animation: beam-spin 8s linear infinite;
}
@keyframes beam-spin { to { --beam-angle: 360deg; } }
```
`@property` 미지원 브라우저에서는 빔이 정지 상태로 보일 뿐 기능 무손실 — §15 프로그레시브 인핸스먼트 원칙에 부합. 흰색 대신 브랜드색 2스탑을 쓰면 컬러 빔.

*레시피 ② 시머 텍스트* — 대기 문구("분석 중…")의 하이라이트 스윕. 순수 CSS, 폐쇄망 OK:
```css
.t-shimmer { position: relative; display: inline-block; color: var(--ink-500); }
.t-shimmer::before {
  content: attr(data-text); position: absolute; inset: 0; pointer-events: none;
  background: linear-gradient(90deg, transparent 40%, var(--ink-900) 50%, transparent 60%);
  background-size: 400% 100%; background-repeat: no-repeat;
  -webkit-background-clip: text; background-clip: text;
  color: transparent; -webkit-text-fill-color: transparent;
  animation: t-shimmer 2s linear infinite;
}
@keyframes t-shimmer { from { background-position: 100% 0; } to { background-position: 0% 0; } }
/* 사용: <span class="t-shimmer" data-text="분석 중…">분석 중…</span> */
```

*레시피 ③ 점묘 오브 (canvas 2D)* — 히어로 비주얼·AI 대기 연출. 원리: 3D 점(위도 링 × 경도 밀도)에 회전 행렬을 적용한 뒤 **z 깊이를 점 크기·잉크 농도·알파에 매핑**해 2D로 찍는다(원근 투영 없는 depth-cueing). 단색 잉크만 쓰면 어느 테마에도 녹고 WebGL이 필요 없다. "검색 중=스캔 회전, 생각 중=궤도, 작성 중=리본"처럼 **AI 상태마다 다른 지오메트리를 부여하는 상태 어휘화**가 가능 (원전이 9종 검증).
canvas 생명주기 필수 규칙 — CSS reduced-motion 가드(§7)는 canvas를 덮지 못하므로 레시피에 내장한다:
- IntersectionObserver + `visibilitychange`로 화면 밖/백그라운드 탭에서 rAF 정지
- `devicePixelRatio`는 2로 캡
- `prefers-reduced-motion`: 애니메이션 대신 **정지 프레임 1장만 렌더** ("단축" 가드의 canvas 버전)
- `role="img"` + `aria-label`(상태 문구) 부여 (§8)
- **속도 상한**: 상호작용 피드백 모션은 ≤300ms. 무한 애니메이션은 히어로·경고 등 의미 있는 곳에만 (corporate_loan_advisor 명문화 원칙).
- **안무는 이름 붙은 타이밍 상수로 명세한다** (corporate_loan_advisor 플라이트 시스템):
```
FLY_ENTER 300ms(중앙 등장) → HOLD 1000ms(스포트라이트 체류) → TRAVEL 600ms(도킹 비행)
카드 여러 장은 STAGGER 200ms 간격
```
매직넘버로 흩어놓지 않고 상수 파일 한 곳에서 관리 — 시연·문서·코드가 같은 숫자를 공유한다.
- **시연 최적화 ≠ 일상 사용**: 심사·시연에서 인상적인 안무(스포트라이트 체류 등 카드당 ~2초)는 매일 쓰는 숙련자에게는 대기 시간이 된다. 반복 빈도가 높은 업무 화면의 연출 모션에는 **숙련자 모드(안무 생략, 즉시 반영) 또는 최초 N회만 재생** 옵션을 함께 설계한다.

**명시적 비표준 (Out of Scope) — 검토 후 채택하지 않기로 결정한 기법**

외부 레퍼런스 분석에서 검토했으나 이 가이드의 표준 범위 **밖**으로 명시한다. 각 프로젝트 DESIGN.md의 Known Gaps("아직 못 정한 것")와 달리, 아래는 **의도적으로 배제한 것**이다 — AI 에이전트와 후속 작업자가 "가이드에 없으니 도입해도 되나?"를 재검토하지 않도록 결정과 이유를 남긴다:

- **WebGL/3D 엔진 (Three.js 등)**: 로컬 번들로 폐쇄망 반입 자체는 가능하나, ① 이 문서의 목적(디자인 비전문가도 재현 가능한 표준)에서 3D 씬·셰이더 제작은 재현 불가능한 스킬이고 ② §15 성능 규율의 검증 범위 밖이며 ③ §11-9 단일 파일 비대화를 필연적으로 유발한다. 연출의 기술 상한은 §7 레시피 ③(canvas 2D 점묘 오브)까지. (검토 원전: igloo.inc — 개념은 레퍼런스 표 참조, 기술은 배제)
- **게임화 내비게이션 (탐험해야 정보가 나오는 구조)**: bruno-simon.com(3D 운전 탐험 포트폴리오)이 원전. 정보 도달 비용을 의도적으로 높여 경험·화제성과 맞바꾸는 설계로, "매체 자체가 실력 증명"인 개인 쇼케이스 맥락에서만 성립한다. §16 대원칙(속도·효율 우선)과 정면 충돌하므로 사내 서비스에는 채택하지 않는다 — 업적·숨김 요소 등 게임 문법의 부분 이식도 업무 화면에는 금지.

---

## 8. 접근성 베이스라인 (전 프로젝트 필수)

분석 결과 가장 자주 누락된 영역. 아래 8개는 예외 없이 적용한다:

1. `:focus-visible { outline: 2px solid var(--brand-500); outline-offset: 2px; }`
2. **아이콘 전용 버튼에 `aria-label`** — 전 프로젝트 공통 취약점이었음. `title`만으로는 부족.
3. 토스트/동적 영역에 `role="status"` / `role="alert"` + `aria-live`
4. `prefers-reduced-motion` 가드 — 단축 방식 (§7)
5. 장식 SVG/이모지에 `aria-hidden="true"`
6. 터치 타깃 **최소 44×44px, 권장 48×48px**, 키보드 활성화(`Enter`/`Space`) — 커스텀 클릭 요소 포함
7. **색 대비 (WCAG AA)** — 본문 텍스트 4.5:1 이상, 대형 텍스트(24px+ 또는 19px 굵게)·UI 컴포넌트 3:1 이상. 표준 토큰의 허용 조합:

| 조합 | 대비(약) | 판정 |
|---|---|---|
| `--ink-900` / `--ink-700` on 흰색·`--ink-50` | 16:1 / 7.5:1 | 본문 OK |
| `--ink-500` on 흰색 | **2.9:1** | **본문·캡션 금지** — AA 미달. 장식적 보조(비활성 힌트)에만 |
| 흰색 on `--brand-500` | 3.7:1 | 대형 텍스트·버튼 라벨(16px 굵게 이상) OK, 소형 본문 금지 |
| 흰색 on `--brand-600` | 5:1 | 본문 OK — **작은 버튼 텍스트는 600 배경 사용** |
| 시맨틱 본색 on 시맨틱 `-bg` | 3:1 내외 | 뱃지(굵은 소형)용 — 문장형 메시지는 더 진한 톤(-700급) 사용 |

8. **hover 전용 정보 전달 금지** — 툴팁·부가 정보가 hover로만 열리면 터치·키보드 사용자는 접근 불가. hover는 보조 신호로만 쓰고, 같은 정보에 클릭/포커스 경로를 함께 제공한다.

반응형 브레이크포인트 표준: **1500px**(1366×768 사내 노트북 대응) / **1024px** / **768px**(사이드바 → 오프캔버스) / **640px** / **480px**. 사내 도구라도 최소 768px 대응은 필수.

---

## 9. 프레젠테이션 & 스토리텔링 지침

프레젠테이션은 **두 장르로 구분**하고 표준을 다르게 적용한다:
- **보고형** (정보 전달 — 임원 보고, 결과 보고): 경진대회 보고 덱 표준. 데이터 밀도와 컴포넌트 재사용성이 우선.
- **피치형** (설득·경연 — 심사, 투자 유치): `corporate_loan_advisor/05_ppt/ppt_최종통합본_v2.html` 표준. 감정 곡선과 무대 연출이 우선.

공통 방법론은 `ppt_skill.md`의 10가지 지침과 경진대회 덱의 자체 선언 원칙을 따른다.

**덱 디자인 3원칙** (경진대회 보고 덱 헤더에 명문화된 것):
> ① 1슬라이드 1메시지 ② 절제된 골드(강조색) 사용 ③ 충분한 여백 + 미세한 깊이

**7비트 내러티브 골격** (the-fans 프레젠테이션이 검증):
`후킹 오프닝(10초 내 패턴 브레이크) → 문제 → 해결책 → 데이터 증명(카운트업) → 반론 선제 대응 → 60단어 요약 슬라이드 → 명확한 CTA`

**임원 보고용 표준 팔레트** (다크 네이비 × 뮤트 골드):
```css
--bg-0: #0a111d; --bg-1: #0f1a2b;
--panel: rgba(255,255,255,0.035);          /* 글래스 패널 */
--gold: #cbab73; --gold-soft: #e2cb98;      /* 채도 낮춘 골드 — 슬라이드당 1회 */
--ink: #eef3f9; --muted: #8395a9;
--line: rgba(255,255,255,0.10);
```
깊이 레시피: 글래스 패널(`backdrop-filter: blur(4px)`) + 헤어라인 보더 + 레이어드 radial-gradient 배경 + 3~5% 투명도의 초대형 워터마크 숫자.

**데이터 시각화는 순수 CSS로**: conic-gradient 도넛, 퍼센트 포지셔닝 간트 타임라인, 퍼널, 2×2 순환 루프 다이어그램 — 차트 라이브러리 없이 전부 구현된 전례가 있다 (폐쇄망 원칙과도 부합).

**피치형 덱 연출 패턴** (ppt_최종통합본_v2에서 확립된 표준):
- **서사-무대 융합**: 배경을 장식이 아니라 **논증 구조**로 쓴다. v2는 문제 제기 구간에서 비가 5단계로 누적 강화되고(문제가 쌓일수록 폭우), 해결 선언에서 3초에 걸쳐 하늘이 개는 전환으로 "대체로 맑음"이라는 팀명 자체를 기승전결로 만들었다. 메타포가 있다면 무대 전체가 그 메타포를 수행하게 하라.
- **누적 환경 레이어**: 분위기 연출(비·안개 등)은 슬라이드 밖의 **독립 고정 레이어**(`position:fixed`, 슬라이드 전환과 무관)에 두고 슬라이드 인덱스에 따라 강도만 조절 — 전환 시 끊김/깜빡임이 없다. 강도 단계는 인덱스 맵(`RAIN_BY_INDEX`)으로 선언적으로 관리.
- **스크롤 공간 내러티브**: 스크롤/슬라이드 진행도를 **공간 이동**(하강·상승, 계절·날씨 변화, 밤→새벽)에 매핑해 "진행할수록 어딘가로 가고 있다"는 감각을 만든다 — 누적 환경 레이어의 확장 (igloo.inc의 빙하 하강 여정이 원전, v2의 비→맑음 전환과 같은 문법). 구현은 3D 카메라가 아니라 **배경 레이어의 강도·색·오프셋 전환**(CSS + IntersectionObserver) 범위 내에서만 — WebGL은 §7 명시적 비표준.
- **타이포 사투리 전환**: 사실 전달은 산세리프(Pretendard), 감정선(인용, 시적 문장, 에필로그)은 명조(고운바탕)로 전환 — 서체가 "지금은 마음의 이야기"라는 신호가 된다.
- **라이브 데모 iframe 게이팅**: 제품 데모를 덱 안에 iframe으로 임베드하고 `postMessage` 프로토콜(`demo-finished`/`demo-advance`)로 진행을 게이팅. 부모의 진행 키/클릭은 데모 종료 전까지 iframe으로 포워딩하고, 데모 중 휠은 잠가 실수 스킵을 방지. 덱과 시연을 한 산출물로 봉합하는 표준.
- **클릭 단계형 카운터**: 하나의 큰 숫자를 클릭마다 단계 진행(270→500→5,000+)시키며 단계별 킥커/캡션을 교체 — 숫자 여러 개를 한 화면에 나열하는 것보다 극적이다. 슬라이드 이탈 시 리셋, `requestAnimationFrame` 정리 필수.
- **대본 내장**: 발표 대본을 각 슬라이드 안에 숨김 요소(`.speaker`, `display:none`)로 내장 — 덱과 대본이 한 파일에서 함께 관리되어 어긋나지 않는다.
- **클릭 진행 우선순위 계약**: 진행 입력(→/Space/클릭)은 ① 데모 게이트 → ② 카운터 단계 → ③ 미공개 `.reveal` 요소 → ④ 다음 슬라이드 순으로 소비 — "티키타카" 연출(질문 표시 후 클릭으로 답 공개)의 기반.

**라이브 데모 연출 지침** (corporate_loan_advisor 애니메이션 타이밍 명세서 — 시연을 "무대 연출"로 설계한 표준):
- **게이트(gate)**: 발표자 멘트가 있는 지점에서 데모를 정지, 클릭 큐로 재개. 멘트 없이 흐르는 구간은 빠르게 자동 재생.
- **유레카 하이라이트는 게이트에서만**: "여기 보세요" 스포트라이트는 정지 지점에만. 흐르는 구간에는 강조 금지.
- **커서 억제(instant 플래그)**: 흐르는 항목은 가상 커서의 이동/클릭/타이핑을 생략 → 커서의 등장 자체가 주의 신호가 된다.
- **침묵 커버리지**: 대기 시간(로딩 등)은 나레이션과 겹치도록 단축·배치해 데드에어를 없앤다.
- 게이트별 직전 멘트 문장까지 명기한 **클릭 큐 시트**를 대본과 함께 관리한다.

---

## 10. 인쇄물 지침 (포스터·공문·리포트)

- 단위는 **pt**, `@page { size: A4 }`, `-webkit-print-color-adjust: exact`
- `@media print`에서 그림자·버튼·화면 전용 UI 제거
- 화면 앱에 인쇄 리포트가 필요하면 **별도 문서 스타일**로 재구성 (corporate_auction_loan의 A4 리포트 분리)
- React 앱의 인쇄는 **Portal 패턴**: `createPortal`로 인쇄 전용 DOM 렌더 + `@media print { #root { display:none } }` + `break-inside-avoid` 청킹 + `@page` 카운터 (corporate_loan_advisor). 인쇄물은 **3톤(그레이/레드/그린)으로 제한**하고 앱 브랜드색은 제외; 고객 전달물에는 공식 브랜드색(IBK 네이비)만.
- 격식 문서: 네이비(`#0b2545`) 구조 + 인장 레드(`#8b0000`)는 숫자·강조에만 — 2색 강조 규율
- 감성 포스터: 순수 CSS 스크랩북 기법 — 비대칭 `border-radius`, 미세 `rotate(-1~1deg)`, clip-path 마스킹 테이프, 하드 오프셋 그림자(`2.5px 4px 0`), radial-dot 종이 질감 (baby-calender 포스터, 9/10)

---

## 11. 안티패턴 — 하지 말 것

실제 프로젝트에서 발견된 실패 패턴. 코드 리뷰 시 체크리스트로 사용:

1. **토큰 선언 후 하드코딩** — `:root`에 변수를 만들어 놓고 컴포넌트에서 hex를 직접 쓰는 것 (prize-calculator, pdf-agent 일부 뷰). 토큰이 있으면 반드시 토큰만 참조.
2. **JS 템플릿 문자열 안의 인라인 스타일** — `innerHTML`로 주입하는 HTML에 하드코딩 색상 (must-go). 클래스로 스타일링.
3. **토큰 블록 페이지별 복붙** — 공유 CSS 파일 하나로 분리 (condo_picker 3중 복붙).
4. **같은 앱에 두 벌의 컴포넌트 시스템** — 레거시 `.modal`과 신형 `.modal-unified` 공존 (mini-HRS). 리팩터링 시 구버전 제거.
5. **아이콘 전용 버튼에 접근 가능한 이름 누락** — 전 프로젝트 공통 결함.
6. **`alert()` / 무의미한 에러 메시지** — 인라인 시맨틱 에러 + 행동 지향 문구로.
7. **z-index 매직넘버** — 이름 붙은 스케일 토큰 사용.
8. **로딩 상태 생략** — 목업이라도 로딩 UI를 함께 설계 (condo_picker의 누락 사례).
9. **단일 HTML 파일 비대화** — 131~525KB 단일 파일은 유지보수 한계. 정식 서비스화 시 컴포넌트 분리 (Vue SFC + composables 구조 권장, pdf-agent 참고).
10. **디자인 스펙 문서와 코드의 괴리 방치** — 팔레트를 교체(Toss 블루→틸)하고도 구버전 `design_system.json`을 남겨두면 후속 작업자가 잘못된 스펙을 참조한다 (corporate_loan_advisor). 시스템이 진화하면 **스펙 문서를 같은 커밋에서 갱신하거나 구버전에 deprecated 표기**.
11. **blur로 폼 진행/제출** — 포커스 이탈을 진행 트리거로 쓰면 의도치 않은 조기 진행이 발생. 명시적 액션(클릭/Enter/버튼)으로만 진행 (§5 진행 트리거 매트릭스).
12. **무거운 무한 애니메이션 + reduced-motion 가드 누락 조합** — 모션이 풍부한 프로젝트일수록 §7 킬스위치가 더 중요하다 (corporate_loan_advisor의 유일한 접근성 공백이었음).

---

## 12. 새 프로젝트 시작 체크리스트

- [ ] **디자인 킥오프 인터뷰 진행 (§14)** — 아래 모든 결정의 입력값. 인터뷰 없이 차용 시스템/팔레트를 임의로 정하지 않는다
- [ ] 서비스 성격에 맞는 차용 시스템 선택 (§1-①) 및 맥락 팔레트 결정 (§1-③) — 해외 시스템이면 awesome-design-md에서 DESIGN.md 가져오기
- [ ] 프로젝트 루트에 DESIGN.md 작성 (§13 표준 목차)
- [ ] §2 표준 토큰 블록을 공유 CSS 파일로 복사, 브랜드 색만 교체
- [ ] Pretendard 로컬 번들 + `word-break: keep-all` + `tabular-nums` 전역 적용
- [ ] 4가지 표준 셸(§4) 중 하나 선택
- [ ] 6개 상태(§6) 화면 설계 — 빈/로딩/진행/성공/에러/오프라인
- [ ] 접근성 8종(§8) 적용 — 색 대비 허용 조합 표 포함
- [ ] `prefers-reduced-motion` 가드(단축 방식) + 이징 토큰 2종만 사용
- [ ] 구현 규율(§15) 준수 — CSS 4레이어, 상태는 aria/data 속성, transform/opacity 애니메이션만, 폰트 서브셋+preload
- [ ] 사용자 만족 패턴(§16) 적용 — 키보드 흐름, 기억하는 UI, 입력 자동화 후보 검토, Undo/자동저장, AI 결과물 근거·초안 표시
- [ ] 출시 전 5분 복도 테스트 (§17) — 동료 2명, 과제만 주고 관찰
- [ ] 성공 지표 before 측정치 기록 (§14 Q8 → §17)
- [ ] 보이스&톤 정의 (§1-⑧: 페르소나, 말투, 이모지 역할표)
- [ ] 인터랙션 계약 문서화 (§1-⑨: 진행 트리거, 요약 포맷, 타이밍 상수)
- [ ] 외부 CDN 의존성 0개 확인 (폐쇄망 배포 가능 여부)
- [ ] preview.html 스타일 카탈로그 작성 (§13) — 토큰 드리프트 검증용
- [ ] 안티패턴 체크리스트(§11) 코드 리뷰

---

## 13. 프로젝트별 DESIGN.md 운영

이 문서(DESIGN_GUIDELINES.md)는 워크스페이스 전역 지침이고, **각 프로젝트는 이를 인스턴스화한 자체 DESIGN.md를 루트에 둔다** (condo_picker에서 검증된 관례, VoltAgent/awesome-design-md의 운영 모델). 마크다운은 AI 코딩 에이전트가 가장 잘 읽는 형식이므로, DESIGN.md가 프로젝트 루트에 있으면 "이 프로젝트의 디자인 언어로 만들어줘"라는 요청이 자동으로 성립한다.

### 표준 목차 (Stitch 포맷 + 자체 확장)

```markdown
# <프로젝트명> DESIGN.md

## 1. Visual Theme & Atmosphere   ← 무드를 한 단락 산문으로
   (예: "따뜻하고 관대한 컨슈머 마켓플레이스. 깨끗한 흰 캔버스 위에
    단일 브랜드 전압 Rausch가 모든 주요 CTA를 이끈다. 타이포의 힘보다
    사진과 여백을 신뢰한다." — 토큰 표로는 전달되지 않는 "느낌"을
    AI와 사람 모두에게 전달하는 섹션)

## 2. Color Palette & Roles       ← hex + 역할 산문 (§2 규칙: 어디에 쓰고 어디에 안 쓰는지)
## 3. Typography                  ← 폰트, 스케일 표, 한국어 규칙
## 4. Layout                      ← 셸 선택(전역 §4), 간격, 그리드
## 5. Depth & Elevation           ← 그림자 티어와 사용처
## 6. Components                  ← 이 프로젝트에서 실제 쓰는 컴포넌트만
## 7. Motion                      ← 이징 토큰, 타이밍 상수, 안무 명세
## 8. Voice & Tone                ← 페르소나, 말투, 이모지 역할표 (§1-⑧)
## 9. Do's and Don'ts             ← 이 프로젝트 고유의 허용/금지 (전역 §11과 별개)
## 10. Known Gaps                 ← 아직 정의 안 된 영역 + 지원 브라우저 기준선
```

**Known Gaps를 반드시 포함한다** — "다크 모드는 미정의", "모바일 레이아웃은 검토 전" 처럼 빈 곳을 명시해야 AI 에이전트가 없는 규칙을 지어내지 않고, 사람도 문서를 완성본으로 오신하지 않는다. **지원 브라우저 기준선**(폐쇄망 PC의 실제 Edge/Chrome 버전)도 여기에 기록한다 — `backdrop-filter`, `:has()`, `text-wrap: balance`, `scrollbar-gutter` 같은 최신 속성의 사용 가능 여부를 판단하는 근거가 된다 (§15).

### preview.html — 살아있는 스타일 카탈로그

DESIGN.md와 쌍으로, 모든 토큰과 핵심 컴포넌트를 한 페이지에 렌더링한 **preview.html**을 프로젝트에 둔다 (awesome-design-md는 모든 시스템을 DESIGN.md + preview.html 쌍으로 제공).

- 내용: 컬러 스와치(토큰명 병기), 타이포 스케일, 그림자/라운드 티어, 버튼/입력/카드/뱃지/토스트 등 핵심 컴포넌트의 전 상태(기본/hover/disabled/에러)
- 반드시 **실제 공유 CSS(토큰 파일)를 그대로 로드**해서 렌더링 — 별도 복사본 금지
- 효과: 최다 빈발 안티패턴인 **"토큰 선언 후 하드코딩"(§11-1)을 눈으로 검증**할 수 있다. 실제 화면과 프리뷰가 다르면 드리프트가 생긴 것. 디자인 리뷰·인수인계 시 첫 화면으로도 사용.
- 다크 테마가 있는 프로젝트는 preview-dark.html 별도 제공.

### 유지 규칙

- 팔레트/토큰이 바뀌면 **같은 커밋에서 DESIGN.md와 preview.html을 함께 갱신** (§11-10 "스펙 문서와 코드 괴리" 방지 — corporate_loan_advisor의 구버전 design_system.json 방치가 반면교사).
- DESIGN.md에는 전역 지침과 중복되는 내용을 다시 쓰지 않는다. 전역과 다른 결정(팔레트 교체, 셸 선택, 프로젝트 고유 컴포넌트)과 그 이유만 기록.

---

## 14. 디자인 킥오프 인터뷰 — 새 프로젝트의 디자인 첫 대화

**목적**: 개발자가 "디자인을 어디서부터 요청해야 할지" 몰라도 되게 한다. 새 프로젝트 설계를 시작하면 **구현에 들어가기 전에** AI(또는 디자인 리드)가 아래 인터뷰를 진행해 디자인 컨셉을 함께 결정하고, 그 결과로 §13 DESIGN.md 초안을 만든다.

### 진행 규칙 (인터뷰어 = AI를 위한 지침)

1. 새 서비스 개발 요청을 받으면 **먼저 이 인터뷰부터** 시작한다. 단, 요청에 이미 답이 들어있는 질문은 건너뛴다 (다 아는데 형식적으로 묻지 않는다).
2. 질문은 한 번에 2~3개씩만. **반드시 선택지를 함께 제시**한다 — 개발자가 디자인 용어를 몰라도 고르기만 하면 되게. 열린 질문은 Q1(서비스 소개)과 Q8(성공 지표)뿐이다.
3. **질문에 디자인 전문용어를 쓰지 않는다.** 상대는 "팔레트·셸·레퍼런스·모션·브랜딩"을 모르는 현업 담당자라고 가정한다. 모든 개념은 상대가 이미 아는 앱(토스, 배달의민족)이나 일상 장면(호텔 라운지, 콘서트 포스터, 연말정산 화면)에 비유해서 묻는다. 아래 질문에 붙은 화살표 매핑(→ §…)과 괄호 속 용어는 **인터뷰어 내부용**이며 입 밖에 내지 않는다.
4. "모르겠어요 / 알아서 해줘"라는 답에는 **추천 기본값을 제시하고 진행**한다. 인터뷰가 심문이 되면 안 된다 — 5분 안에 끝나는 가벼운 대화가 목표.
5. 인터뷰가 끝나면 아래 **디자인 브리프** 형식으로 요약해 확인받고, 승인되면 DESIGN.md 초안을 생성한다. 브리프가 곧 DESIGN.md의 "Visual Theme & Atmosphere" 초안이 된다.

### 질문 8개 — "묻는 말"은 현업 담당자에게 그대로 소리 내어 읽어도 되는 대사다

**Q1. 서비스 한 줄 소개** (유일한 주관식)
> 묻는 말: "누가, 어떤 상황에서, 무엇을 하려고 쓰는 서비스인가요? 옆 팀 동료에게 소개하듯 한 문장이면 충분해요."
> 예시 답: "영업점 직원이 고객 상담 중에 경락자금 대출 가능 여부를 빠르게 확인하는 도구"

**Q2. 산출물 유형** → §4 셸 / §9 덱 장르 / §10 인쇄 지침 결정
> 묻는 말: "만들려는 게 다음 중 어디에 가장 가까운가요?"
> ① **매일 쓰는 업무 화면** — 사내 조회·심사 시스템처럼 업무 시간 내내 켜두고 쓰는 것
> ② **간단한 계산기·조회 도구** — 이자 계산기처럼 값을 넣으면 결과가 나오는 것
> ③ **행사·홍보 페이지** — 사내 이벤트 안내처럼 한 번 구경하고 마는 것
> ④ **발표 자료** — 이어서 하나 더 묻는다: "결과를 정리해서 보고하는 자리인가요(→ 보고형), 심사위원이나 경영진 마음을 움직여야 이기는 자리인가요(→ 피치형)?"
> ⑤ **종이로 뽑는 것** — 포스터, 공문, 안내장

**Q3. 사용 빈도와 사용자** → 모션 수준·학습 비용 설계 결정
> 묻는 말: "쓰는 사람 입장에서, 이 화면을 얼마나 자주 열게 되나요?"
> ① **매일 수십 번** — 메신저처럼 손에 익어야 하는 화면 (→ 화려한 효과를 빼고 속도 최우선)
> ② **가끔, 어쩌다 한 번** — 연말정산 화면처럼 들어올 때마다 새로 헤매게 되는 것 (→ 친절한 안내·설명을 곳곳에)
> ③ **딱 한 번** — 청첩장이나 초대장처럼 첫인상이 전부인 페이지 (→ 첫 화면 연출에 투자)

**Q4. 원하는 첫인상** → 차용 시스템·팔레트 결정 (§1-①③)
> 묻는 말: "완성된 화면을 처음 본 사람이 어떤 느낌을 받으면 좋겠어요? 평소 아는 앱이나 장면 중에서 골라주세요."
> ① **토스 앱 같은 느낌** — 흰 바탕에 파란색, 은행처럼 깔끔하고 믿음직한 (→ Toss 블루 계열)
> ② **배달의민족 같은 느낌** — 민트색, 친근하고 말 걸기 편한 (→ 배민 민트 계열)
> ③ **에어비앤비 같은 느낌** — 사진이 큼직하고 따뜻한, 여행 앱 감성 (→ Airbnb 계열)
> ④ **호텔 라운지·임원 보고서 느낌** — 남색 바탕에 금색 포인트, 묵직하고 격조 있는 (→ 네이비×골드)
> ⑤ **콘서트 포스터 느낌** — 검은 바탕에 쨍한 형광색, 에너지 넘치는 (→ 네온 온 블랙)
> ⑥ **개발자 프로그램 같은 느낌** — 눈이 편한 어두운 화면, 차분한 작업 도구 (→ 다크 에디터, 민트 액센트)
> ⑦ **애플 신제품 소개 페이지 느낌** — 거의 검은 무대 위에 제품 하나만 조명을 받는 (→ 미니멀 크래프트 다크(§1-③) + §7 후킹 연출 레시피. 제품 자랑·데모 랜딩용)

**Q5. 브랜드 요소** → 메타포 시스템 결정
> 묻는 말: "팀 이름이나 서비스 이름, 밀고 싶은 콘셉트가 있나요? 예를 들어 '대체로 맑음'이라는 팀은 '날씨'를 테마로 잡아서, 화면 곳곳에 해가 뜨고 구름이 걷히는 연출까지 이어졌어요. 그런 식으로 밀고 싶은 이미지나, 이미 쓰고 있는 로고·색이 있으면 알려주세요."
> 있으면 그것을 시각 시스템 전체로 확장한다 (§1-③, 레퍼런스 표의 igloo.inc 사례). 없으면 AI가 서비스 성격에 맞는 이름+콘셉트 2~3안을 제안하고 고르게 한다.

**Q6. 배포·사용 환경** → 기술 제약 결정
> 묻는 말 (4개 모두 예/아니오로):
> · "인터넷이 안 되는 회사 내부망 PC에서만 쓰나요?" (→ 폐쇄망 §1-④)
> · "휴대폰으로도 열어보나요, 사무실 PC에서만 보나요?" (→ 모바일 대응 §8)
> · "종이로 뽑아서 쓸 일이 있나요?" (→ 인쇄 §10)
> · "회의실 스크린이나 빔프로젝터에 띄워서 보여줄 일이 있나요?" (→ 시연 대응 §4-C·§9)
> (사내 도구 기본값: 내부망 ○, 모바일 ×, 1366×768 노트북 대응 ○)

**Q7. 참고 레퍼런스**
> 묻는 말: "'이거랑 비슷하게 만들어주세요'라고 보여줄 만한 화면이 있나요? 사내 시스템이든, 평소 쓰는 앱이든, 어디서 본 페이지든 좋아요. 캡처해서 보여주셔도 돼요."
> 없으면 역할별 표준 표(문서 상단)의 해당 유형 표준을 따른다.

**Q8. 성공 지표** → §17 출시 후 검증의 before/after 기준 (주관식)
> 묻는 말: "이 서비스가 대박 났다고 상상해 보세요. 지금과 비교해서 뭐가 얼마나 좋아져 있을까요?"
> 예시 답: "엑셀 수작업 30분이 30초로", "심사의견 작성 2시간이 30분으로"
> **지금 정해야 before 측정치가 남는다** — 출시 후에 지표를 찾으면 개선을 증명할 수 없다. 답이 어려우면 "지금 이 일을 하는 데 시간이 얼마나 걸리세요?"로 바꿔 묻는다.

### 디자인 브리프 템플릿 (인터뷰 결과 요약 → 확인용)

```markdown
## 디자인 브리프 — <프로젝트명>
- 서비스: <Q1 한 줄>
- 유형/셸: <Q2 → §4 셸> · 사용 패턴: <Q3>
- 차용 시스템: <Q4 → 예: 배민 민트> / 브랜드 색: <hex>
- 메타포/네이밍: <Q5 → 예: "대체로 맑음" 날씨 서사>
- 톤: <Q4 형용사> + 페르소나 <예: 옆자리 사수> (§1-⑧)
- 환경: 폐쇄망 <○/×> · 모바일 <○/×> · 인쇄 <○/×>
- 레퍼런스: <Q7>
- 모션 수준: <절제/표준/연출형> (Q3 기반)
- 성공 지표: <Q8 → 예: "수작업 30분 → 30초"> (출시 후 §17에서 측정)
```

**확인받을 때도 전문용어 금지 (진행 규칙 3의 연장)**: 브리프는 기록용 문서(DESIGN.md의 재료)이므로 위 형식 그대로 두되, 현업 담당자에게 확인받을 때는 브리프를 그대로 읽지 말고 쉬운 말로 요약해 들려준다 — "토스 앱 느낌의 파란 화면으로, 사무실 PC 전용으로, 가끔 쓰는 분들도 헤매지 않게 안내를 넉넉히 넣어서 만들게요. 맞나요?"

이 브리프에 개발자가 "좋아요" 하면 → §13 DESIGN.md 초안 생성 → §12 체크리스트의 나머지 항목 진행. **인터뷰 → 브리프 → DESIGN.md → 구현**이 새 프로젝트의 표준 시퀀스다.

---

## 15. 구현 규율 — CSS 아키텍처 & 성능 (퍼블리셔 표준)

디자인이 좋아도 구현이 흐트러지면 유지보수에서 무너진다. 마크업/CSS 작성 시 아래 규율을 따른다.

### CSS 레이어 구조

공유 CSS는 4레이어로 분리하고 이 순서로 로드한다:

```
1. tokens.css      §2 :root 토큰 (+ 역할 토큰 레이어)
2. base.css        리셋, body/타이포 전역(§3), 포커스 링, reduced-motion 가드
3. components.css  버튼/카드/모달/폼 등 (§5)
4. utilities.css   .num, .identifier, 간격 헬퍼 등 소수만
```

단일 HTML 파일 프로젝트여도 `<style>` 안에서 주석 배너로 같은 4단 순서를 유지한다 — 나중에 분리할 때 그대로 잘라내면 된다.

### 네이밍 & 상태 컨벤션

- 클래스는 케밥케이스, `블록-요소` 2단계까지만 (`.card`, `.card-title`). 3단계가 필요해지면 컴포넌트를 분리하라는 신호.
- 변형은 접미사 클래스: `card-{tool}`, `uz-{tool}` (§5 기능별 액센트 테마).
- **JS가 제어하는 상태는 aria/data 속성을 셀렉터로 직접 스타일링한다**: `[aria-pressed="true"]`, `[aria-expanded="true"]`, `[data-state="done"]`, `[disabled]`. 접근성 트리와 시각 상태가 자동 동기화되어 "`.active` 클래스는 붙었는데 aria는 안 바뀐" 버그 클래스가 사라진다. 순수 시각 상태(스크롤 여부 등)만 `.is-*` 클래스 허용.

### 성능 규칙

- **애니메이션·트랜지션은 `transform`/`opacity`만** — `width`/`height`/`top`/`left`/`box-shadow` 전환은 리플로우·리페인트를 유발한다. 그림자 호버는 `::after`에 hover 그림자를 미리 그려두고 opacity만 전환. `transition: all` 금지 — 대상 속성을 명시 (§2 토큰 주석 참조).
- **아코디언 높이 전환**은 `max-height` 트릭 대신 CSS Grid `grid-template-rows: 0fr → 1fr` — 이징 왜곡이 없고 콘텐츠 길이와 무관하다.
- **한글 웹폰트**: 서브셋 woff2 사용(전체 글리프 대비 용량 수분의 일) + `font-display: swap` + `<link rel="preload" as="font" crossorigin>`. 웨이트별 여러 파일보다 가변(Variable) 폰트 1파일이 낫다 (Pretendard Variable — image-agent 검증).
- **레이아웃 시프트(CLS) 방지**: 이미지에 `width`/`height` 속성 명시, 동적 삽입 콘텐츠는 예약 슬롯 패턴(§5 플라이트), 라우트 전환 시 스크롤바 폭 점프는 `html { scrollbar-gutter: stable }` (미지원 브라우저는 `overflow-y: scroll` 폴백 — pdf-agent 검증).
- 스크롤 연동 효과는 scroll 리스너 대신 **IntersectionObserver** (§4-D 크롬 킷에서 이미 표준).

### 브라우저 기준선

프로젝트 시작 시(§14 Q6) 폐쇄망 PC의 실제 브라우저 버전을 확인해 DESIGN.md Known Gaps에 기록한다. **기준선이 미확인이면** `backdrop-filter`, `:has()`, `text-wrap: balance`, `scrollbar-gutter` 등 최신 속성은 **없어도 기능이 성립하는 장식(프로그레시브 인핸스먼트)으로만** 사용한다 — 핵심 기능·레이아웃을 최신 속성에 걸지 않는다.

---

## 16. 사용자 만족 패턴 — 만족도는 속도·안전·신뢰에서 나온다

**대원칙: 실제 사용자의 만족도·편의성이 시각 완성도보다 우선한다.** 매일 쓰는 업무 도구의 만족도는 예쁨이 아니라 "빠르고, 덜 귀찮고, 믿을 수 있음"에서 나온다.

### 16-1. 효율 — 사용자의 시간을 아끼는 설계

- **키보드 퍼스트**: 화면 진입 시 첫 입력에 자동 포커스, Tab 순서는 시선 흐름과 일치하게 설계, Enter로 다음 단계까지 이어지는 입력 흐름(§5 진행 트리거와 연동), 자주 쓰는 액션에 단축키. 마우스 없이 주 과업이 완주 가능해야 한다.
- **기억하는 UI**: 최근 입력값·마지막 사용 상태를 복원하고(localStorage/IndexedDB), 자주 쓰는 항목을 우선 정렬한다. "이어하기"를 첫 화면의 1급 버튼으로 (must-go 최근 검색, corporate_loan_advisor 이어하기·자동저장에서 검증).
- **입력 자동화 — 타이핑을 시스템이 대신할 수 없는지 먼저 묻는다**: 붙여넣기 파싱(prize-calculator의 인사카드 붙여넣기 → 자동 집계), 파생값 자동 계산(corporate_loan_advisor AutoCalculation), 포맷 자동 정리(사업자번호 하이픈, 금액 콤마). 사용자가 옮겨 적는 순간이 보이면 그것이 자동화 대상이다.
- **반응성 임계값** (Nielsen 표준 — §6 로딩 규칙의 하한선):

| 응답 시간 | 요구 사항 |
|---|---|
| ~100ms | 즉각으로 느껴짐 — 클릭/입력 피드백은 이 안에 |
| ~1초 | 흐름 유지 — 로딩 UI를 띄우지 않는다 (깜빡이는 스피너가 오히려 느려 보이게 함) |
| 1초 이상 | 진행 표시 필수 |
| 10초 이상 | 진행률 + 경과 시간 + **취소 버튼** + 완료 후 다음 행동 안내 |

### 16-2. 에러 예방 & 안전망 — 에러 처리(§6)보다 에러가 안 나게

- **Undo가 확인 대화상자보다 낫다**: "정말 삭제하시겠습니까?"는 습관적으로 클릭되어 보호 효과가 없다. 삭제·이동은 즉시 실행 + **"실행 취소" 토스트(5~10초)**를 기본으로. 확인 대화상자는 되돌릴 수 없는 파괴적 작업(전체 초기화, 외부 발송)에만 남긴다.
- **자동 저장 + 저장 상태 표시**: 긴 입력 흐름은 자동 저장을 기본으로 하고 "저장됨 · N분 전"을 상시 표시 (corporate_loan_advisor TopBar 검증) — 사용자가 저장을 기억해야 하는 부담 자체를 제거한다.
- **입력 제약으로 원천 차단**: 검증으로 잡지 말고 입력 단계에서 막는다 — 자유 텍스트 대신 선택지(§5 칩/셀렉트), 불가능한 날짜는 picker에서 비활성화, 숫자 필드는 숫자만 받게. "에러 메시지가 자주 뜨는 필드"는 검증 강화가 아니라 입력 방식 재설계 신호다.
- **이탈 보호**: 미저장 작업 중 창 닫기/새로고침 시 `beforeunload` 경고. 단, 자동 저장이 있으면 경고 대신 조용한 복구(재진입 시 이어하기)가 더 나은 경험.

### 16-3. AI 결과물 신뢰 UX

AI 기능이 들어가는 서비스의 만족도는 정확도만큼 **신뢰 프레이밍**이 좌우한다 (recommend-agent, corporate_loan_advisor에서 검증):

- **근거를 함께 보여준다**: 어떤 입력/규칙에서 이 결론이 나왔는지 추론 사슬 표시 (recommend-agent ReasoningChain, Raw JSON 토글). "AI가 그랬어요"가 아니라 "이 근거로 이렇게 판단했어요".
- **"초안"임을 명시한다**: AI 산출물은 "심사의견 **초안**"처럼 최종 판단이 사용자에게 있음을 프레이밍하고, **수정 가능한 형태**로 제공 (금융 사내 도구에서 특히 중요 — 책임 소재의 UX).
- **데이터의 기준일/출처를 표시한다**: "6개월 평균 대비", "2026-07 기준" — 판단 재료의 신선도를 사용자가 알 수 있게.
- 실패 시 몰래 폴백하지 않는 원칙(§6)과 한 몸 — 신뢰는 정직한 실패 처리에서 완성된다.

---

## 17. 사용성 검증 루프 — 완성도는 출시로 끝나지 않는다

사내 도구의 최대 장점은 **사용자가 옆자리에 있다**는 것이다. 이 장점을 프로세스로 만든다.

### 출시 전 — 5분 복도 테스트

- 동료 1~2명에게 **과제만 주고**("이 화면에서 ○○를 해보세요") 설명 없이 조용히 관찰한다. 설명하고 싶어지는 순간이 곧 UI가 실패한 지점.
- 막히는 지점·머뭇거림·잘못 누른 곳을 그대로 수정 목록으로. 5명이면 주요 문제 대부분이 드러난다 — 대규모 테스트가 필요한 게 아니라 **0명과 2명의 차이가 결정적**이다.
- 관찰 항목: 첫 성공까지 걸린 시간, 도움 없이 완주했는가, 표정이 굳는 지점.

### 출시 후 — 피드백 채널과 지표

- **앱 안에 한 줄 피드백 버튼**: 이동 없이 그 화면에서 바로 (현재 화면 정보 자동 첨부). 피드백 비용이 낮을수록 진짜 불만이 들어온다.
- **성공 지표는 §14 인터뷰(Q8)에서 미리 정의**한다 — 출시 후에 지표를 찾으면 before 측정치가 없다. "상담·작성 2시간 → 30분"(corporate_loan_advisor) 같은 before/after는 킥오프 때 측정 계획이 있었기에 가능한 서사다.
- 정기 관찰: 재사용률(다시 찾는가), 완주율(중간 이탈 지점), 자주 발생하는 에러 — 이 셋이 만족도의 대리 지표.
- 관찰 결과로 확인된 개선 패턴은 이 문서에 역반영한다 (부록 개정 이력).

---

## 부록 — 개정 이력

이 문서는 c:\Projects 전체 프로젝트(14개)의 실제 구현 코드 분석으로 작성되었습니다. 새로운 패턴이 검증되면 이 문서에 추가하고 아래 이력에 기록하세요.

| 일자 | 차수 | 내용 |
|---|---|---|
| 2026-07-08 | 초판 | 13개 프로젝트 전수 분석 — 철학 7원칙, 표준 토큰, 셸 4종, 상태 카탈로그, 안티패턴 9종 |
| 2026-07-08 | 2차 | corporate_loan_advisor 분석 반영 — 철학 ⑧⑨ 추가, 아코디언 플로우·플라이트 안무·경고 비침투·누적 코칭 팁, 모션 타이밍 상수, 라이브 데모 연출, Portal 인쇄, 안티패턴 10~12 (레퍼런스: `corporate_loan_advisor/03_guide_md/cf_design_system.md`, `애니메이션_타이밍_명세서_for_ClaudeCode.md`) |
| 2026-07-08 | 3차 | 평가 축 분리 — "역할별 표준 레퍼런스" 표 도입. 업무 UX 표준 = corporate_loan_advisor(최우선), the-fans는 목업임을 명기하고 시각 시스템 참조로 한정 |
| 2026-07-08 | 4차 | 프레젠테이션 표준 2장르 분리 — 보고형 = 경진대회 보고 덱 / 피치형 = `ppt_최종통합본_v2.html`. §9에 피치형 연출 패턴 7종 추가 (v2의 reduced-motion 누락은 재사용 시 보완) |
| 2026-07-09 | 5차 | VoltAgent/awesome-design-md 반영 — 차용 소스 카탈로그(§1-①), 색-역할 서술(§2), §13 프로젝트별 DESIGN.md 운영(Stitch 목차 + Known Gaps + preview.html) |
| 2026-07-09 | 6차 | §14 디자인 킥오프 인터뷰 — 선택지 기반 7문항 → 디자인 브리프 → DESIGN.md 초안의 표준 시퀀스 수립 |
| 2026-07-09 | 7차 | 전문 퍼블리셔 관점 검수 반영 — **[모순 수정]** `overflow-wrap` 페어 누락(§3), reduced-motion 제거→단축 방식(§7), `transition: all` 토큰 폐기(§2), 이징 2종 표기 통일, 시맨틱 네이밍 success/warning/danger/info로 통일(§2). **[신규]** WCAG 색 대비 기준 + 토큰 허용 조합 표·hover 전용 정보 금지(§8, 6종→8종), 역할 토큰 레이어 다크 모드 전략(§2), 데이터 테이블·아이콘 시스템(§5), 입력 검증 계약(§6), §15 구현 규율(CSS 4레이어·aria/data 상태 셀렉터·transform/opacity 전용·폰트 서브셋·CLS 방지·브라우저 기준선), 목차 & 사용 순서, 개정 이력 표 전환 |
| 2026-07-09 | 8차 | 대원칙("실사용자 만족도·편의성 우선") 반영 — 신규 §16 사용자 만족 패턴(키보드 퍼스트·기억하는 UI·입력 자동화·반응성 임계값 100ms/1s/10s, Undo>확인창·자동저장·입력 제약 원천 차단·이탈 보호, AI 결과물 근거·"초안" 프레이밍·기준일 표시), 신규 §17 사용성 검증 루프(출시 전 5분 복도 테스트, 앱 내 한 줄 피드백, 재사용률·완주율 관찰, 개선 패턴의 문서 역반영), §14에 Q8 성공 지표(before 측정치 확보), §1-⑧ 마이크로카피 규칙, §6 첫 사용 경험("첫 성공까지 3분") |
| 2026-08-13 | 9차 | 외부 레퍼런스(beam/orbs.jakubantalik.com) 번들 분석 반영 — **연출 계층에 격리, 시스템 표준(§2 토큰·§5 컴포넌트·§15 본칙)은 변경 없음.** §7 후킹 연출 레시피 3종 신설(보더 빔 conic-gradient+mask, 시머 텍스트, 점묘 오브 canvas 2D + 상태 어휘화) + 공통 사용 조건(화면당 1회·업무 화면 금지·폰트 로컬 치환·소형 요소 한정 filter 전환 예외·width 전환 금지 유지·canvas 생명주기/reduced-motion 정지 프레임 규칙), §1-③ 미니멀 크래프트 다크 맥락 추가, §14 Q4 선택지 ⑦, 레퍼런스 표에 외부 행(쇼케이스 경고 병기), §6 로딩 상호참조 |
| 2026-08-13 | 10차 | 외부 랜딩 3종(sasasa.com·igloo.inc·bruno-simon.com) 분석 반영 — 9차와 동일 원칙(연출 계층 격리, 시스템 표준 불변). 레퍼런스 표에 igloo.inc 추가(브랜드 메타포 무대화 — 개념만 참조, WebGL은 배제), §7 공통 조건에 인트로 연출 규칙(스킵 수단 필수·재방문 자동 생략), §9 피치형에 스크롤 공간 내러티브(CSS 레이어 범위 한정), §7 말미 **"명시적 비표준" 신설** — WebGL/3D 엔진·게임화 내비게이션을 검토 후 의도적 배제로 명문화 (배제 이유 포함, Known Gaps와 구분) |
| 2026-08-13 | 11차 | §14 킥오프 인터뷰 전면 비전문가 언어화 — 질문 8개를 "그대로 소리 내어 읽는 대사(묻는 말)" 형식으로 재작성: 모든 선택지를 아는 앱·일상 장면 비유로 교체(토스 앱/배달의민족/호텔 라운지/콘서트 포스터/연말정산 화면/청첩장/애플 신제품 페이지), 전문용어·§매핑은 괄호 속 인터뷰어 내부용으로 격리. 진행 규칙에 "질문에 디자인 전문용어 금지" 신설(규칙 3), 브리프 확인 단계도 쉬운 말 요약으로 진행하도록 명시 |
