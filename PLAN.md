# AX-BRM 요청 접수 서비스 — 구현 플랜 (v0.1, 2026-09-01)

> 원본: `ai-brm-roadmap-v0.6.html` (AX-BRM 의사결정 및 지원 로드맵)
> 디자인: `DESIGN_GUIDELINES.md` · 인증: `SSO연동가이드.md`

---

## 0. 한 줄 정의

**현업 직원이 "내 업무 고민"을 쉬운 질문에 답하는 것만으로 AX-BRM 상담 신청서를 완성하고, AX-BRM은 그 답변을 로드맵의 판정 기준(지원 갈래·데이터 케이스·배포지·연계 방식·예상 기간)으로 자동 번역해 받아 검토·의견·목업까지 한 곳에서 관리하는 서비스.**

핵심 설계 원칙: **요청자는 로드맵 용어를 한 번도 보지 않는다.** 요청자는 자기 업무 언어로 답하고, 시스템(규칙 엔진)이 BRM 언어로 번역한다.

---

## 1. HTML 로드맵 분석

### 1-1. 구조 (10개 절, 3구간)

| 구간 | 절 | 내용 | 성격 |
|---|---|---|---|
| 인입·개발 | 00 전체 흐름 | 접수 → 행외 개발 → 행내 반입 3구간 연결도 | 개요 |
| | 01 접수 채널 셋 | CH-1 상시 접수 / CH-2 붐업페스타 수상작 / CH-3 BRM 역제안. 채널별 "이미 확보된 것 / 먼저 확인할 것". **착수 판정**: 보류·조건부 보류 / AI 활용(GENI·알대리로 종료) / 개발 검증(바이브코딩 → 바이브랩스 → AI HUB) | **요청자 질문 대상** |
| | 02 행외 개발·개발용 데이터 | 데이터 필요 없음 / **모양만 같으면 됨(표준)** / 실제 값 필요 · 비식별 실데이터(+3~5주, 승인 불확실). 데이터 계약: 어댑터 한 층만 교체 | **요청자 질문 대상**(데이터 필요성·실제값 필요성) |
| 반입·배포 | 03 반입 판정 흐름 | AI-HUB? → 행내 데이터? → 온라인/배치? → 양? | 판정 트리(자동 도출) |
| | 04 결정할 두 가지 | 배포지와 데이터 연계는 **독립** 결정. 착수 시 가판정 → 개발 착수 전 확정 | 설계 원칙 |
| | 05 배포지 결정 | Q1 AI-HUB 규격? Q2 비-AI-HUB 불가피 사유 4개(기존 시스템 결합/특수 HW/망·구역/자원 상한) → 재설계 or IT 이관 | **요청자 질문 일부**(기존 시스템 결합·특수 장비) |
| | 06 환경 정합화 12항목 | 런타임·패키지·CDN·외부 API·OS·문자셋·DB 방언·인증·네트워크·시간대·라이선스·반입 절차 | BRM 내부(요청자 비노출) |
| | 07 데이터 연계 방식 결정 | 온라인 → 마트 조회(1~2주)/연계 API(8주+) · 배치 → 파일 수령(2~3주)/1회 일괄(3~4주)/BDP 직접(4~6주). 개인정보 +2~4주 | **요청자 질문 대상**(실시간성·양·개인정보) |
| 실행 | 08 연계 방식별 협의 | 방식별 협의 상대·챙길 것·리드타임 표 | 자동 도출(협의처·기간) |
| | 09 14주 로드맵 | W-3~W10 표준 시나리오 | 자동 도출(예상 일정) |
| | 10 협의처 지도 | 9개 협의처 × 시점 × 안건 | 자동 도출(협의처 목록) |

### 1-2. 로드맵에서 뽑아낸 "판정 변수" (시스템이 계산해야 하는 것)

| 판정 변수 | 값 | 결정 입력(요청자 답변) |
|---|---|---|
| 접수 채널 | CH-1 / CH-2 / CH-3 | 시작 계기 |
| 지원 갈래 | AI 활용(GENI / 알대리) / 개발 검증 / 보류 | 원하는 결과물 형태 |
| 개발용 데이터 케이스 | A 가상 / B 합성 / C 비식별 실데이터 | 데이터 필요 여부, 실제 값 필요 여부 |
| 배포지 가판정 | AI-HUB(기본) / 비-AI-HUB 검토 / 재설계 | 기존 시스템 결합, 특수 장비, 인터넷 필요 |
| 데이터 연계 방식 | 없음 / 마트 조회 / 연계 API / 파일 수령 / 1회 일괄 / BDP 직접 | 행내 데이터 필요, 실시간성, 양, 빈도 |
| 개인정보 심의 | 필요 / 불필요 | 개인 식별 정보 포함 여부 |
| 요건 확정 후 개발 예상 기간 | 주 단위 범위 (참고용 초안) | 위 판정의 리드타임 합산 — 요청서 작성·요건 협의 구간은 제외(2026-09-08) |
| 협의처 목록 | 10절 표에서 해당 항목 | 위 판정 |
| 요청자가 준비할 것 | 체크리스트 | 채널별 "먼저 확인할 것" + 데이터 케이스별 요구 |

### 1-3. 리드타임 규칙 (07·08·02절에서 추출)

- 행내 데이터 불필요: 반입 점검 2~3주
- 온라인·마트 조회: 1~2주 / 온라인·연계 API: 8주+ (개발부터 연계까지 기간이 추가로 필요 — 경고)
- 배치·소량 파일 수령: 2~3주 / 배치·대량 1회 일괄: 3~4주 / 배치·대량 정기 BDP: 4~6주
- 실제 값 필요(비식별 실데이터): +3~5주 (승인 불확실 — 경고)
- 개인정보 포함: 모든 경로 +2~4주
- 표준 시나리오 전체(AI-HUB + BDP + 개인정보): 접수~오픈 14주

---

## 2. 비전공자(요청자) 관점 재정의

### 2-1. 용어 사전 — 질문·답변·결과 화면에서 쓰는 말

| 로드맵 용어(BRM 내부) | 요청자에게 보이는 말 | 비유 / 한 줄 설명 |
|---|---|---|
| AX-BRM | AI 상담 창구 | "AI로 업무를 바꾸고 싶을 때 먼저 찾는 옆자리 베테랑 사수" |
| 접수 채널 CH-1/2/3 | 이 아이디어가 시작된 계기 | 부서 고민 / 붐업페스타 수상 / BRM 제안 |
| 지원 영역 · AI 활용 | 이미 있는 AI 도구로 해결 | **기성복** — 바로 입을 수 있는 옷(GENI·알대리) |
| 지원 영역 · 개발 검증 | 새 프로그램을 직접 만들어 보기 | **맞춤복** — 치수 재고 만드는 데 시간이 걸림 |
| GENI (LLM·RAG) | 사내 챗봇형 AI (글 요약·초안·문서 질문답변) | "사내 규정을 다 읽은 비서에게 물어보기" |
| 알대리 (RPA·AI-OCR) | 반복 클릭·입력·서류 읽기를 대신하는 자동화 | "정해진 순서로 일하는 로봇 직원" |
| 바이브코딩 / 바이브랩스 | AI에게 말로 설명해 프로그램을 만드는 방식 (은행 밖 개발 환경) | "설계도 없이 말로 설명하면 집을 지어주는 건축 AI" |
| 행외 개발 | 인터넷이 되는 밖에서 먼저 만들기 | 은행 안(폐쇄망)에서는 AI 개발 도구를 못 쓰기 때문 |
| 행내 반입 | 밖에서 만든 프로그램을 은행 안으로 들여오기 | **입국 심사** — 짐 검사(보안 점검)를 통과해야 함 |
| AI-HUB | 사내 AI 프로그램 공용 건물 | **오피스 빌딩 입주** — 전기·보안·청소가 갖춰져 있어 빠름. 단독주택(비-AI-HUB)은 다 따로 지어야 해서 기간 산정 불가 |
| 데이터 필요 없음(가상 데이터) | 실제 데이터 없이 입력값만으로 동작 | 계산기 — 숫자를 넣으면 답이 나옴 |
| 모양만 같으면 됨 | 데이터 "모양(형식)"만 같으면 되는 경우 | **모델하우스** — 가구 배치·크기는 진짜, 안의 물건은 가짜 |
| 실제 값 필요(비식별 실데이터) | 실제 값의 분포·특성이 꼭 필요한 경우 | **얼굴을 가린 실제 사진** — 심사(+3~5주)를 통과해야 밖으로 나갈 수 있음 |
| 데이터 계약 / 어댑터 | (비노출) | — |
| 온라인 연계 | 화면을 여는 순간 최신 데이터가 필요 | **창구** — 고객이 앞에 앉아 있을 때 즉시 조회 |
| 배치 연계 | 하루 한 번(또는 정기적으로) 갱신되면 충분 | **조간 신문** — 아침에 한 번 받아 하루 종일 봄 |
| 마트 조회 권한 | 이미 정리된 사내 데이터 창고에서 바로 조회 | **편의점** — 진열돼 있으면 바로 살 수 있음 (1~2주) |
| 연계 API | 원천 시스템에 실시간으로 직접 물어보기 | **전화 주문** — 저쪽 시스템도 준비해야 해서 오래 걸림 (8주+) |
| 파일 수령 (.dat/CSV) | 필요한 데이터를 파일로 정기 전달받기 | **택배** (2~3주) |
| BDP 직접 연결 · 일배치 | 매일 자동으로 데이터가 흘러들어오는 전용 통로 | **수도 배관 공사** — 처음엔 오래(4~6주), 이후엔 자동 |
| 개인정보 이용 심의 · 가명처리 | 고객 이름·주민번호·계좌번호 등 "누군지 알 수 있는 정보" | 포함되면 어떤 경로든 +2~4주 심사 |
| 환경 정합화 12항목 | (비노출 — BRM 검토 화면에만) | — |
| 협의처 (데이터혁신부 등) | 함께 협의해야 하는 부서 | 결과 화면에 "누구와 무엇을 협의하게 되는지" 안내 |
| 리드타임 | 요건 확정 후 개발 예상 기간 | 항상 "약 N~M주(참고용 초안)"로 범위 표기 · 요건·난이도에 따라 달라짐을 함께 표기 |
| 착수 판정 / 보류 / 조건부 보류 | 검토 결과: 진행 / 보완 필요 / 보류 | BRM 검토 화면 용어 |

### 2-2. 로드맵 절 → 요청자에게 어떻게 묻는가

| 로드맵 절 | 요청자 노출 | 묻는 방식 |
|---|---|---|
| 01 접수 채널 | ○ | "이 아이디어는 어떻게 시작됐나요?" 3지선다 |
| 01 착수 판정(갈래) | ○ (간접) | "원하는 결과가 다음 중 어디에 가까운가요?" — 답에서 GENI/알대리/개발검증 자동 도출 |
| 02 데이터 케이스 | ○ (간접) | "실제 사내 데이터가 필요한가요?" → "실제 값 그대로여야 하나요, 모양만 같아도 되나요?" |
| 03·07 연계 트리 | ○ (간접) | "화면을 열 때마다 최신이어야 하나요?" "양은 어느 정도인가요?" |
| 04 두 가지 결정 | × | 결과 화면의 "배포 방식 / 데이터 연결 방식" 두 카드로만 표현 |
| 05 배포지 | ○ (간접) | "기존 사내 시스템에 붙여야 하나요?" "특수 장비·인터넷이 필요하나요?" |
| 06 환경 정합화 | × | BRM 검토 화면 체크리스트 |
| 08 협의 내용 | ○ (결과) | "함께 협의할 부서" 카드 |
| 09 14주 로드맵 | ○ (결과) | "예상 진행 순서와 기간(초안)" 타임라인 |
| 10 협의처 지도 | ○ (결과) | 협의처 카드에 시점 표시 |

---

## 3. 인터뷰 질문 흐름 (한 질문 → 답변 → 다음 질문)

> 진행 트리거(디자인 가이드 §5): 선택형은 클릭 즉시 다음 질문, 주관식은 Enter 또는 "다음 →" 버튼. blur 진행 금지.
> 모든 선택지에는 한 줄 설명(비유)이 붙고, "잘 모르겠어요" 선택지는 항상 제공 → BRM 검토 시 확인 항목으로 표시.

### A. 어떤 일인가요 (공통)

| # | 질문(그대로 읽는 대사) | 형식 | 매핑 |
|---|---|---|---|
| Q1 | 하고 싶은 일을 한 문장으로 적어주세요. 옆 팀 동료에게 설명하듯 편하게요. | 주관식 | 제목/한 줄 소개 |
| Q2 | 이 아이디어는 어떻게 시작됐나요? ① 우리 부서에서 늘 겪는 불편 ② 붐업페스타 수상작 ③ AX-BRM에서 먼저 제안받음 | 선택 | 채널 CH-1/2/3 |
| Q3 | 지금은 이 일을 어떻게 하고 계세요? (사용하는 시스템·엑셀·수작업 등) | 주관식 | 현재 업무 방식 |
| Q4 | 이 일에 지금 시간이 얼마나 걸리나요? (예: 1건 30분, 주 3시간) | 주관식(짧게) | 성공 지표 before |
| Q5 | 완성되면 누가 쓰나요? ① 나 혼자 ② 우리 팀 ③ 우리 부서 ④ 여러 부서/영업점 ⑤ 전 직원 | 선택 | 사용자 범위 |
| Q6 | 이 일은 일 년에 몇 건이나 발생하나요? ① 매일 수시로 ② 주에 몇 번 ③ 월에 몇 번 ④ 분기·반기 작업 ⑤ 연간 작업 | 선택 | 발생 빈도 |

### B. 어떤 도움이 맞을까요 (갈래 판정)

| # | 질문 | 형식 | 매핑 |
|---|---|---|---|
| Q7 | 원하는 결과가 다음 중 어디에 가까운가요? ① 글을 요약·초안 작성·문서에서 답 찾기 ② 반복되는 클릭·입력·서류 읽기를 자동으로 ③ 화면이 있는 새 프로그램(조회·계산·관리 도구) ④ 잘 모르겠어요, 상담받고 싶어요 | 선택(카드) | ①→GENI ②→알대리 ③→개발 검증 ④→BRM 상담 |

- ①②(AI 활용) 선택 시 → Q8-a "지금 GENI/알대리를 써본 적 있나요?" → **C·D 건너뛰고 E로**. 결과 화면에 "기성복(사내 AI 도구)으로 바로 도울 수 있어요" 안내.
- ③④ → C로 진행.

### C. 데이터가 필요한가요 (개발 검증 갈래)

| # | 질문 | 형식 | 매핑 |
|---|---|---|---|
| Q9 | 이 프로그램이 동작하려면 행내 데이터(고객·거래·계좌 등)가 필요한가요? ① 아니요, 사용자가 입력한 값만으로 결과가 나와요(계산기처럼) ② 네, 행내 데이터를 읽어야 해요 ③ 잘 모르겠어요 | 선택 | ①→데이터 없음 ②→Q10 |
| Q10 | 데이터가 얼마나 자주 새로워져야 하나요? ① 화면을 열 때마다 최신이어야 해요(실시간) ② 하루 한 번(또는 정기적으로) 갱신되면 충분해요 ③ 한 번만 받으면 돼요 ④ 잘 모르겠어요 | 선택 | ①온라인(시나리오 3) ②③배치(시나리오 4) — 2026-09-07 "실시간성"과 "1회/정기" 두 질문을 통합 |
| Q11 | 다루는 데이터 양은 어느 정도인가요? ① 1만 건 이하 ② 10만 건 이하 ③ 그 이상 ④ 잘 모르겠어요 | 선택 | 소량→파일 수령 / 대량→BDP(정기)·1회 일괄(한 번만). 온라인이면 참고용 |
| Q12 | 외부에서 만들 때 실제 값이 필요한가요? ① 데이터의 모양(항목·형식)만 같으면 돼요 — 모델하우스처럼 ② 실제 값의 분포·특이 사례가 있어야 해요 ③ 잘 모르겠어요 | 선택 | ①→모양만 같으면 됨 ②→실제 값 필요(경고: +3~5주) |
| Q13 | 완성된 뒤 다루게 될 데이터에 개인정보가 들어가나요? (고객 이름·주민번호·계좌번호 등. 만들 때 가짜 값을 써도 완성 후 실제 데이터에 있으면 해당) ① 아니요 ② 네 ③ 잘 모르겠어요 | 선택 | 개인정보 심의(+2~4주) |
| Q14 | 데이터가 어떤 시스템에 있는지 아시나요? (시스템 이름·담당 부서, 모르면 건너뛰기) | 주관식(선택) | 원천 시스템(참고용) |

### D. 어디에서 돌아가나요 (배포지)

| # | 질문 | 형식 | 매핑 |
|---|---|---|---|
| Q16 | 기존 사내 시스템(코어뱅킹·영업점 단말 등) 안에 끼워 넣어야 하나요? ① 아니요, 따로 열어 쓰는 웹 화면이면 돼요 ② 네, 기존 시스템 안에서 동작해야 해요 ③ 잘 모르겠어요 | 선택 | ①AI-HUB ②비-AI-HUB 검토 사유① |
| Q17 | 특별한 장비(스캐너·카메라·전용 단말)나 외부 인터넷 연결이 필요하나요? ① 아니요 ② 네 (무엇인지 적기) | 선택+주관식 | 비-AI-HUB 사유②③, 정합화 항목 04 |

### E. 마무리 (공통)

| # | 질문 | 형식 | 매핑 |
|---|---|---|---|
| Q18 | 이 일을 함께 진행할 담당자는 누구인가요? ① 제가 직접 ② 다른 담당자(이름) ③ 아직 미정 | 선택+주관식 | 수행 부서·담당자 |
| Q19 | 부서장님도 이 요청을 알고 계신가요? ① 네, 동의하셨어요 ② 아직 말씀 전이에요 | 선택 | 부서장 착수 동의 |
| Q20 | (개발 검증만) AI에게 말로 설명해서 직접 만들어보는 방식에 참여할 수 있나요? ① 네, 시간을 낼 수 있어요 ② 만들어주시면 사용만 하고 싶어요 | 선택 | 행외 개발 인력 |
| Q21 | 이 서비스가 대박 났다고 상상해 보세요. 지금과 비교해서 뭐가 얼마나 좋아져 있을까요? | 주관식 | 성공 지표 after |
| Q22 | 언제까지 있으면 좋을까요? ① 급해요(1~2개월) ② 올해 안 ③ 여유 있어요 | 선택 | 희망 시기 |
| Q23 | 참고할 화면·자료(캡처, 엑셀 양식, 비슷한 서비스)가 있으면 올려주세요. 없어도 괜찮아요. | 첨부(선택) | 참고 자료 |

질문 수: AI 활용 갈래 약 12개(3분), 개발 검증 갈래 약 20개(6~8분).

### 3-2. 요약 결과 화면 (모든 답변 후)

1. **요청 요약 카드** — Q1 제목, 요청자·부서(SSO 자동), 현재 방식·목표
2. **자동 판정(초안)** 카드 4장 — "AX-BRM 검토 후 확정됩니다" 문구 필수(§16-3 초안 프레이밍)
   - 도움 방식: 기성복(GENI/알대리) 또는 맞춤복(새 프로그램)
   - 데이터 준비 방식: 계산기형 / 모델하우스형 / 실데이터 심사형
   - 데이터 연결 방식: 없음 / 편의점(마트) / 택배(파일) / 배관(BDP) / 전화(API)
   - 배포 방식: 사내 AI 공용 건물(AI-HUB) / 별도 검토
3. **요건 확정 후 개발 예상 기간(참고용 초안)** — 리드타임 규칙 합산 범위(요건 협의 구간 제외) + 간단 타임라인(09절 축약)
4. **함께 협의할 부서** — 10절 협의처 중 해당 항목
5. **미리 준비하시면 빨라지는 것** — 체크리스트(담당자 지정, 부서장 동의, 데이터 항목 목록 등)
6. **답변 다시 보기/수정** — 각 행 연필 아이콘 → 해당 질문으로 이동
7. **[AX-BRM에 신청하기]** 버튼 → 접수번호 발급 + 완료 화면("다음은 이렇게 진행돼요")

### 3-3. 판정 규칙(요약)

```
갈래     = Q7①→GENI, Q7②→알대리, Q7③→개발검증, Q7④→상담필요
데이터   = Q9①→A ; Q10①→B ; Q10②→C(+3~5주) ; 모름→B(확인필요 플래그)
개인정보 = Q11②→심의(+2~4주)
연계     = A→없음(2~3주)
          온라인(Q10①) → 마트조회(1~2주) [연계API 8주+는 BRM이 상향 판정]
          배치·소량(Q11①) → 파일수령(2~3주)
          배치·대량·한 번만(Q10③) → 1회일괄(3~4주)
          배치·대량·정기 → BDP(4~6주)
배포지   = Q16②/Q17② → 비-AI-HUB 검토(사유 기록·기간 산정 불가) ; 그 외 AI-HUB
예상기간 = 인입·구체화 3주 + 개발(현업 참여도별 4~6주) + 연계 리드타임 + 가산항 → 범위로 표기
협의처   = 갈래·데이터·개인정보·배포지 조합으로 10절 표에서 선택
```

---

## 4. 시스템 설계

### 4-1. 기술 스택 (weekly-ai와 동일 계열 → SSO 코드 재사용)

| 층 | 선택 | 이유 |
|---|---|---|
| 프론트 | Vue 3 + Vite + TypeScript, 단일 SPA | SSO 가이드의 참조 구현(weekly-ai)이 Vue/Vite. `index.html` IIFE·`ssoToken.ts`·`api.ts` 패턴 그대로 이식 |
| 백엔드 | Node.js + Express, `server/ssoAuth.js`(내장 crypto만) | 폐쇄망 — 의존성 최소. 가이드 §5 검증 순서 4단계 유지 |
| DB | PostgreSQL (JSONB로 답변 저장) | 가이드에 23505 언급 → weekly-ai가 PostgreSQL. 통계 쿼리 용이 |
| 파일 | 서버 디스크 `uploads/` + DB 메타데이터 | 목업 HTML·참고자료. 해시명 저장, 원본명은 DB |
| 배포 | AI-HUB 컨테이너(웹 UI + API), 도메인·SSO 연동 | 로드맵 05절 "AI-HUB 기본 경로" 자체 준수 |
| 외부 CDN | **0개** | Pretendard woff2 로컬 번들, 아이콘 인라인 SVG, 차트 순수 CSS/SVG |

### 4-2. 질문 트리·판정 규칙은 "데이터"로 분리

- `src/domain/questions.ts` — 질문 정의(id, 대사, 선택지+설명, 형식, `next` 분기 규칙, 코칭 팁)
- `src/domain/rules.ts` — 답변 → 판정 변수 계산(순수 함수, 단위 테스트 가능)
- `src/domain/leadtime.ts` — 리드타임 표·가산 규칙
- `src/domain/glossary.ts` — §2-1 용어 사전(ⓘ 툴팁·결과 화면 설명에 사용)
- 요청 레코드에 `questionnaire_version` 저장 → 질문이 바뀌어도 과거 요청 해석 가능

### 4-3. DB 스키마 (초안)

```sql
users            (employee_no PK, name, org_cd, org_nm, role  -- requester|brm|admin
                  , created_at, last_login_at)
requests         (id PK, req_no UNIQUE  -- 'BRM-2026-0001'
                  , title, status, channel
                  , requester_employee_no FK, requester_name, requester_org_cd, requester_org_nm  -- 신청 시점 스냅샷(통계용)
                  , questionnaire_version, answers JSONB, judgement JSONB  -- 자동 판정 결과 스냅샷
                  , submitted_at, created_at, updated_at)
request_reviews  (id PK, request_id FK, reviewer_employee_no FK
                  , decision  -- 접수진행|보완요청|보류|AI활용안내|협의 종결
                  , feasible  -- 가능|조건부|불가|검토중
                  , approach TEXT, opinion TEXT, estimated_weeks_min, estimated_weeks_max
                  , judgement_override JSONB  -- BRM이 자동 판정을 수정한 값
                  , created_at)               -- 의견은 누적(이력)
status_history   (id PK, request_id FK, from_status, to_status, changed_by, note, changed_at)
attachments      (id PK, request_id FK, kind  -- mockup|reference|other
                  , file_name, mime, size, storage_path, version, note
                  , uploaded_by, uploaded_at)
comments         (id PK, request_id FK, author_employee_no, body, created_at)  -- 요청자↔BRM 문의 (Phase 2)
```

상태 흐름: `draft(작성중) → submitted(신청완료) → reviewing(검토중) → {hold(보류/보완요청) | accepted(진행확정) | guided(사내도구 안내로 종결) | rejected(협의 종결)} → developing(개발중) → done(완료)` — 컨셉 목업 단계는 폐지(2026-09-03): 목업은 필수 절차가 아니라 필요시 첨부로만 등록한다

통계 축: 요청일자(일/주/월), 요청부서(org_cd), 요청자, 채널, 지원 갈래, 상태, 데이터 케이스, 배포지, 평균 처리 기간(submitted→첫 review, → done). CSV 내려받기 포함.

### 4-4. API (초안)

```
POST /api/auth/sso                      { token } → { user, role }
GET  /api/me
GET  /api/questionnaire                 현재 버전 질문 트리
GET  /api/requests?mine=1 | (brm) ?status=&org=&from=&to=&channel=
POST /api/requests                      draft 생성/자동저장  (PUT /api/requests/:id 도 동일)
POST /api/requests/:id/submit           신청 확정 → req_no 발급
GET  /api/requests/:id                  상세(답변·판정·리뷰·첨부·이력)
POST /api/requests/:id/reviews          (brm) 의견 등록 + 상태 변경
POST /api/requests/:id/attachments      (brm/requester) multipart 업로드
GET  /api/attachments/:id               다운로드 / GET .../preview  목업 iframe용(sandbox)
GET  /api/stats?from=&to=&groupBy=      (brm) 집계
GET  /api/stats/export.csv
```

권한: 모든 보호 API는 호출마다 `Authorization: Bearer <SSO토큰>` 재검증(가이드 §7). 요청자는 자기 요청만, BRM은 전체. BRM 여부는 **우리 DB의 users.role**(토큰 role 사용 금지).

### 4-5. SSO 연동 (가이드 §10 체크리스트 그대로)

- `index.html` IIFE 부트스트랩: URL `?token=` → sessionStorage(`aiBrmSsoToken`) → 없으면 포탈 팝업. 토큰 URL 즉시 제거, 15초 무한루프 가드, 호스트 분기(localhost→`/popup.html` 목업, `devai`→개발 포탈, 그 외 운영)
- `server/ssoAuth.js`: HS256 고정 → HMAC 재계산·timingSafeEqual → exp 필수·skew 60s → `id`·`name` 필수. 키 = `SSO_JWT_SIGN` 앞 32자 base64 디코딩 24바이트
- `POST /api/auth/sso`: 검증 → 사번 6자리 검사 → users upsert(23505→409) → 로그 1줄(조직|이름)
- 클라이언트 토큰 폐기: **401/400만**. 네트워크 오류·5xx는 폐기 금지 (`api.ts` 거절/네트워크 구분)
- 조직 정보: HR 연동 없이 시작 → 토큰 orgCd/orgNm 사용. 신청 시점 값을 requests에 스냅샷
- `public/popup.html` 목업 + vite 빌드 시 `dist/popup.html` 자동 삭제 플러그인
- 포탈 운영팀 협의 필요: 팝업 경로 등록, 리디렉트 URL, sign 키 수령

### 4-6. 화면 목록

**요청자**
1. 홈 — "새 요청 시작하기"(1급 버튼) · 작성 중 이어하기 · 내 요청 목록(상태 뱃지)
2. 인터뷰 — 중앙 단일 컬럼(880px) 아코디언 질문 플로우 + 상단 도트 스테퍼(A~E 5구간) + 우측 사이드바 "지금까지의 판정 미리보기"·코칭 팁(경고 비침투 원칙) + 자동저장 표시 "저장됨 · 방금"
3. 요약 결과 — §3-2 구성 → [AX-BRM에 신청하기]
4. 신청 완료 — 접수번호, 다음 단계 안내, "홈으로 / 요청 상세 보기"
5. 요청 상세 — 상태 타임라인, BRM 의견, 목업 미리보기(iframe sandbox), 첨부, 문의 남기기(Phase 2)

**AX-BRM**
6. 접수함 — 좌측 사이드바 셸. 필터(상태·부서·채널·기간·갈래), 정렬, 검색. 신규 건 강조
7. 검토 화면 — 좌: 답변 전체(질문 대사 그대로 + 답) / 우: 자동 판정 + 환경 정합화 12항목 체크 + **의견 등록 폼**(결정·실현 가능성·진행 방식·예상 기간·자동 판정 수정) + 상태 변경 + 목업 HTML 업로드(버전 누적, 미리보기)
8. 통계 대시보드 — 기간 선택, KPI 타일(접수·진행·완료·평균 처리일), 월별 추이(CSS 바), 부서별·채널별·갈래별 분포, CSV 내려받기
9. 관리 — BRM 담당자(role) 지정, 질문 트리 버전 확인

### 4-7. 디자인 결정 (DESIGN_GUIDELINES 적용)

킥오프 인터뷰 §14를 이 요청에서 도출된 답으로 채움:

| 항목 | 결정 |
|---|---|
| 유형/셸 | 요청자: **A 도구형 셸 – 중앙 단일 컬럼**(가끔 쓰는 사람 → 안내 넉넉히) / BRM: **A 도구형 셸 – 고정 사이드바**(매일 쓰는 사람 → 속도) |
| 차용 시스템·색 | §2 표준 토큰 골격 + 브랜드색을 로드맵 HTML의 **틸 그린(`--hub` #0A6772 계열)**으로 교체 — BRM 문서·서비스 정체성 연속. (corporate_loan_advisor의 틸 교체 전례) |
| 페르소나 | "옆자리 베테랑 사수" = AX-BRM 상담사. 친근한 존댓말 ~요체, 버튼은 결과가 보이는 동사("저장하고 다음", "AX-BRM에 신청하기") |
| 핵심 컴포넌트 | 아코디언 질문 플로우(완료 행: 우측 볼드 요약 + 연필) · 진행 트리거 매트릭스 · radio-as-card 선택지 · 누적 코칭 팁 💬 · 경고 비침투(사이드바) · 도트 스테퍼 · 상태 뱃지(시맨틱 페어) · 데이터 테이블(sticky 헤더, tabular-nums) |
| 6개 상태 | 빈 상태(첫 방문: 퀵 시작 버튼) · 로딩 · 진행(업로드) · 성공(접수 완료) · 에러(상태코드→행동 번역) · 오프라인(자동저장 로컬 폴백) |
| 접근성 | §8 8종 전부. 시맨틱 색 + 이모지 + 텍스트 3중 부호화. `--ink-500` 본문 금지 |
| 환경 | 폐쇄망 ○ · 모바일 × (768px까지는 대응) · 인쇄 ○(요약 결과 A4 인쇄) · 1366×768 대응 |
| 산출물 | 프로젝트 루트 `DESIGN.md`(§13 목차, Known Gaps 포함) + `preview.html` 스타일 카탈로그 |

---

## 5. 구현 단계

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| **P0 설계 고정** | `questions.ts` 질문 트리 확정, `rules.ts` 판정 규칙 + 단위 테스트, DB 스키마 SQL, `DESIGN.md` + `preview.html`, 프로젝트 골격(Vite+Express) | 규칙 함수가 로드맵 03·05·07 트리와 1:1 대응함을 테스트로 증명 |
| **P1 요청자 흐름** | **랜딩(홈, §6-A)** → 인터뷰(자동저장) → 요약 → 신청 → 완료 → 내 요청 상세. 로컬 목업 로그인(`popup.html`) | 로컬에서 신청 1건이 DB에 저장되고 접수번호 발급 |
| **P2 BRM 흐름** | 접수함, 검토 화면, 의견 등록(이력), 상태 변경, 목업 HTML 업로드·버전·sandbox 미리보기 | 요청자 화면에서 BRM 의견·목업이 보임 |
| **P3 통계** | 대시보드(CSS 차트), 필터, CSV 내려받기 | 요청일자·부서·요청자·채널·갈래·상태별 집계 |
| **P4 운영 준비** | SSO 실토큰 검증, 포탈 등록 협의 항목 정리, 빌드 시 popup 제거, CDN 0 확인, 접근성 점검, 5분 복도 테스트 2명 | 폐쇄망 빌드 산출물 + 배포 체크리스트 |

---

## 6-A. 서비스 랜딩 페이지

### 목적
"AI를 잘 모르는 직원이 도움을 요청하러 오는 첫 화면". 겁먹지 않게, 5분이면 끝난다는 걸 보여주고, 첫 클릭까지 이끄는 것이 유일한 목표. 페르소나 = 옆자리 베테랑 사수(§1-⑧).

### 디자인 레퍼런스 (styles.refero.design 비교 결과)

**1순위 — User Interviews** (`styles.refero.design/style/376baf20-9ace-405d-bf4a-086016f2b1e3`)
"hand-drawn research field notes on warm paper" — 따뜻한 오프화이트 종이 위에 **딥 틸(#1c5d5f)** 단일 액션색, 세리프 헤드라인, 사람을 그린 손그림 라인 일러스트, 민트·블러시 파스텔 카드, 그림자 없음, 필 버튼.
- 채택 이유: ① 브랜드색이 로드맵 HTML 틸(#0A6772)과 같은 계열 → 서비스 전체와 자연스럽게 이어짐 ② "연구자가 사람을 인터뷰하는" 서비스의 스타일 — 우리 서비스도 **인터뷰**가 핵심 ③ 은행 사내 도구가 요구하는 신뢰감(학구적·차분)과 다정함(손그림 사람, 종이 질감)을 동시에 충족 ④ 파스텔 카드 2색(민트/블러시)이 "기성복 vs 맞춤복" 두 갈래 표현에 딱 맞음
- 이식 방식: 팔레트·컴포넌트 규칙(필 버튼 48px, 무그림자, 눈썹 라벨 모노 대문자, 손그림 일러스트)만 차용. 폰트는 폐쇄망 로컬 번들로 대체 — 세리프 헤드라인 **MaruBuri(마루 부리, 무료·자체 호스팅 가능)**, 본문 **Pretendard**, 눈썹 라벨 **D2Coding**

**2순위 — Say Briefly** (`/style/8b91f4c9-74e5-4925-90a3-3dd31fd5725e`)
"warm cream paper, forest green + highlighter yellow" — 형광펜으로 단어를 칠한 헤드라인, 포스트잇 카드(민트/블러시/틸), 손으로 만든 느낌. 더 발랄하고 젊음. "사수가 형광펜으로 표시해준 메모" 메타포가 좋으나 1순위보다 격식이 낮아 은행 사내 톤에는 살짝 가벼움. 형광펜 하이라이트 헤드라인 기법은 1순위에 **부분 차용**.

**3순위 — Getharvest / Slite 계열** (`/style/1eee9aa2-…`, `/style/607c2098-…`)
크림 캔버스 + 따뜻한 오렌지 단일 액센트 + 세리프 히어로. "approachable professionalism". 안전하고 검증된 SaaS 문법이지만 우리 브랜드 틸과 색이 충돌하고 개성이 약함.

검토 후 제외: Duolingo(가장 다정하지만 아동용 스토리북 — 은행 도구엔 과함), Notion(따뜻하지만 블루 계열·노션 클론 인상), Officevibe(HR 에세이 — 다정함보다 격식), Tally(회색 종이 — 차가움), HoneyBook(전면 노랑 — 과시적), Cursor·ElevenLabs(절제된 편집 톤 — 차갑고 개발자 지향), Slush(맥시멀 스티커), Gleap(무채색 건축적).

### 랜딩 페이지 구성 (스크롤 순서)

| # | 섹션 | 내용 · 카피 방향(~요체) | 컴포넌트 |
|---|---|---|---|
| 1 | 상단 내비 | 로고 "AX-BRM" · 내 요청 · **[상담 시작하기]** 필 버튼 | 얇은 sticky 바 |
| 2 | 히어로 | 눈썹 "AX디지털추진부 · AI 상담 창구" / 세리프 헤드라인 "**AI, 잘 몰라도 괜찮아요.** 하고 싶은 일만 말해주세요." / 부제 "전문 용어 없이 12~20개 질문에 답하면, 어떤 도움이 맞는지 AX-BRM이 정리해 드려요." / CTA "5분 상담 시작하기" + 보조 "어떻게 진행되나요?" / 우측: 두 직원이 마주 앉아 메모하는 손그림 SVG | 2컬럼, 로그인 사용자는 "○○○님, 안녕하세요" 개인화 |
| 3 | 신뢰 띠 | "질문 12~20개 · 약 5분 · 전문 용어 없음 · 중간 저장돼요 · 부서장 결재 불필요" | 눈썹 라벨 + 점 구분 |
| 4 | 공감 카드 | "이런 고민 있으셨죠?" — ① 엑셀에 같은 걸 매일 옮겨 적어요 ② 서류 수십 장을 눈으로 읽어요 ③ 규정·매뉴얼 찾는 데 한참 걸려요 ④ 이런 게 있으면 좋겠는데 말할 곳이 없어요. **카드 클릭 = 해당 프리셋으로 인터뷰 시작**(§6 퀵 프리셋) | 민트/블러시 카드 4장 |
| 5 | 진행 순서 | "이렇게 진행돼요" — ① 편하게 답하기(5분) → ② 정리된 요약과 판정 초안 확인 → ③ AX-BRM이 검토하고 연락(영업일 3일 내) → ④ 함께 만들기 | 4단계 수평 스테퍼 + 손그림 아이콘 |
| 6 | 두 갈래 안내 | "기성복이 맞을 수도, 맞춤복이 필요할 수도" — 기성복(GENI·알대리로 이번 주 안에) / 맞춤복(새 프로그램, 함께 만들며 수 주) 비교 카드 | 2컬럼 파스텔 카드 |
| 7 | FAQ | 데이터가 없어도 되나요? / 개발을 전혀 못 하는데요? / 얼마나 걸리나요? / 신청만하면 진행되나요? | 아코디언(§5) |
| 8 | 마지막 CTA | "고민만 있어도 충분해요. 나머지는 함께 정리해요." + [5분 상담 시작하기] + "담당자에게 직접 문의" | 풀폭 틸 밴드(화면당 강조 1회 원칙 유지 — 히어로 CTA와 동일 색) |
| 9 | 푸터 | AX디지털추진부 · 담당자 · 버전 | — |

### 설계 결정
- **랜딩 = 로그인 후 첫 화면(홈)**. SSO 가이드 §4-4(토큰 없으면 무조건 포탈)를 지키므로 비로그인 공개 페이지는 두지 않는다. 포탈에 이미 로그인된 직원에게 왕복은 보이지 않고, 대신 히어로에 이름 인사·"작성 중인 요청 이어하기"를 넣어 다정함을 더한다. 요청 이력이 있는 사용자는 히어로 아래 "내 요청 현황" 띠가 추가된다.
- 랜딩은 **연출형이 아니라 표준 모션**(§14 Q3 "가끔" 사용자 → 안내 우선). 스태거 리빌 + reduced-motion 가드만.
- 일러스트는 인라인 SVG 손그림(폐쇄망, 이미지 에셋 0). 사진·스톡 금지(레퍼런스 규칙과 일치).
- 파스텔 카드는 한 줄에 같은 색 2장 이상 두지 않음(Say Briefly 규칙 차용).

## 7. 구현 현황 (2026-09-01)

| 단계 | 상태 | 검증 |
|---|---|---|
| P0 설계 고정 | ✅ | `shared/questions.js`(23문항·5구간·분기) · `shared/rules.js`(판정·리드타임·협의처·준비물) · `tests/rules.test.js` 14건 통과 · `DESIGN.md` + `preview.html` · DB 스키마 |
| P1 요청자 흐름 | ✅ | 랜딩 → 인터뷰(아코디언·자동저장·사이드 판정) → 요약 → 신청(접수번호) → 완료 → 내 요청 → 상세. Edge headless E2E 통과 |
| P2 BRM 흐름 | ✅ | 접수함(필터·검색) → 검토(답변·판정·의견 등록·상태 전이·판정 조정) → 목업 HTML 업로드·버전·sandbox 미리보기 → 코멘트. API 스모크 통과 |
| P3 통계 | ✅ | KPI · 월별 · 부서/상태/채널/갈래/데이터케이스/연계/배포지 · 요청자 상위 · CSV |
| P4 운영 준비 | ◐ | SSO 검증 모듈(가이드 §5 4단계) + 테스트 7건 · IIFE 부트스트랩 · popup 빌드 삭제 · CDN 0 확인 · 프로덕션 서빙 확인. **남은 것**: 포탈 등록·실토큰 검증, PostgreSQL 어댑터 실행 검증(로컬 PG 없음 → SQLite 기본), 복도 테스트 |

스택 확정: Vue 3 + Vite 7 + TS / Express 5 / **`node:sqlite` 기본**(의존성 0) · `DATABASE_URL` 설정 시 `pg` 어댑터 · 폰트 Pretendard·Noto Serif KR·D2Coding 로컬 번들.

## 8. 신청 이후 진행 단계 설계 · 플래그 기반 자동 상태 전이 (계획)

### 8-1. 요청자에게 보이는 진행 단계 (6단계 주 경로)

```
① 신청 ─▶ ② 검토 ─▶ ③ 확정 ─▶ ④ 목업 ─▶ ⑤ 개발 ─▶ ⑥ 완료
             │
             ├─ ⏸ 보류·보완 요청  (② 에서 멈춤, 보완되면 ② 로 복귀)
             ├─ ✅ 사내 도구 안내로 종결 (기성복 — ③ 대신 종결)
             └─ ⛔ 협의 종결
```

화면: 카드·목록·상세 헤더에 `ProgressSteps` 컴포넌트(점 6개 + 완료 체크 + 현재 강조 + 보류 ⏸). 종결 2종은 주 경로 밖의 뱃지로.

### 8-2. 단계별 "완료 조건 플래그" — 이 플래그가 채워지면 상태가 자동으로 넘어간다

| 현재 → 다음 | 전이 조건(플래그) | 누가 세우나 | 방식 |
|---|---|---|---|
| ① 신청 → ② 검토 | `review.opened` — BRM이 검토 화면에서 요청을 처음 열었을 때 | 시스템(BRM 열람 시 자동 기록) | 자동 |
| ② 검토 → ③ 확정 / ⏸ 보류 / ✅ 종결 / ⛔ 협의 종결 | `review.decision` — 검토 의견의 결정값 | BRM(의견 등록) | 자동(현행) |
| ⏸ 보류 → ② 검토 | `hold.resolved` — 요청자가 보완 답변(코멘트 또는 참고자료 업로드)을 남기면 | 요청자 행동 | 자동 |
| ③ 확정 → ⑤ 개발 | 수동 전이 유지 — 컨셉 목업 **단계 폐지**(2026-09-03): 목업은 필요시 첨부로만. `mockup.*` 플래그 설계는 목업 첨부의 확인·수정요청 UX로만 재활용 검토 | BRM | 수동 |
| ⑤ 개발 → ⑥ 완료 | 체크리스트 3개 모두 ✔ — `dev.data_linked`(데이터 연계 확정) · `dev.imported`(반입·보안 점검 완료) · `dev.opened`(오픈) | BRM(체크) | 마지막 체크 시 자동 |
| ⑤ 개발 (세부) | `dev.data_linked` 등 개별 플래그는 상세 화면 "진행 체크리스트"로 요청자에게도 노출 | BRM | 표시 |

원칙: **상태 컬럼을 직접 바꾸는 API는 없애고, 플래그를 세우는 API만 남긴다.** 상태는 플래그에서 파생(`deriveStatus(flags, prev)`)되며, 파생 결과가 달라질 때만 `status_history`에 "자동 전환 · 사유=플래그명"으로 기록한다. 예외적 수동 조정은 관리자만(사유 필수).

### 8-3. 구현 계획

1. **DB** — `request_flags(request_id, flag, value TEXT, set_by, set_by_name, set_at, note)` 테이블(플래그별 최신 1행 + 이력은 `status_history`와 별개로 `flag_history` 또는 upsert 전 백업). `requests.status`는 파생값 캐시로 유지(목록·통계용).
2. **도메인** — `shared/stages.js`: 단계 정의(6단계 + 종결 2), 플래그 목록, `deriveStatus(flags, current)` 순수 함수 + 단위 테스트(모든 전이 경로).
3. **서버** — `POST /api/requests/:id/flags { flag, value, note }` (권한: 플래그마다 requester/brm 지정) → 저장 → `deriveStatus` → 변경 시 status·history 갱신 → 응답에 `{ status, changed, flags }`. 기존 `/status` 수동 전이는 admin 전용으로 축소. `review.opened`는 `GET /api/requests/:id`를 BRM이 호출할 때 자동 기록. 리뷰 등록·첨부 업로드·코멘트 핸들러가 해당 플래그를 함께 세운다(`mockup.uploaded`, `hold.resolved`).
4. **클라이언트** — 요청자 상세: 목업 카드에 **[이 컨셉으로 진행해 주세요] / [수정 요청]** 버튼, 개발 단계에는 "진행 체크리스트" 읽기 전용 표시. BRM 검토: 개발 체크리스트 3항목 체크박스(저장됨), 상태 변경 칩 제거(관리자만 노출).
5. **알림(선택)** — 상태 전환 시 요청자·BRM에게 사내 메일/메신저 훅 1줄(`server/notify.js`, env로 on/off). 폐쇄망 발송 수단 확인 후.
6. **통계** — 단계별 체류 시간(플래그 set_at 차이) → "검토까지 평균 N일, 목업 확인까지 M일" KPI 추가.

## 6. 확인이 필요한 사항 (가정하고 진행, 다르면 알려주세요)

1. **스택**: Vue 3 + Vite + Node/Express + PostgreSQL — weekly-ai와 동일 계열로 가정. (SQLite 등 경량 DB 선호 시 P0에서 변경 가능)
2. **weekly-ai 참조 파일**(`server/ssoAuth.js`, `index.html` IIFE, `ssoToken.ts`, `api.ts`, `popup.html`, `vite.config.mjs`)을 받을 수 있으면 파일째 복사. 없으면 가이드 스펙대로 새로 구현하고 실토큰으로 검증
3. **브랜드 색**: 로드맵 HTML의 틸(#0A6772) 계열로 가정. Toss 블루 유지 원하면 토큰 1곳만 교체
4. **열람 범위**: 요청자는 자기 요청만, BRM은 전체 — 같은 부서 요청 열람 허용 여부는 미정
5. **BRM 담당자 식별**: 사번 목록을 DB `users.role`로 관리(초기값은 env 시드). 토큰의 role은 사용하지 않음
6. **목업 첨부**: HTML 단일 파일 기본, zip(다중 파일)은 Phase 2 검토. 미리보기는 `iframe sandbox` 격리
7. **첨부 용량 상한**: 20MB 가정
