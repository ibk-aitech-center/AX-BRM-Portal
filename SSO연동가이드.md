# IBK AiPortal SSO 연동 공통 가이드

주간업무회의시스템(weekly-ai)에서 검증·운영된 SSO 연동 방식을 다른 시스템에 재사용하기 위한 가이드다.
여기 적힌 규칙 대부분은 **실제 운영 사고를 겪고 확정된 것**이므로, 새 시스템에서 임의로 생략하지 말 것.

---

## 1. 전체 구조 (한 장 요약)

```
┌──────────┐  ① 접속(토큰 없음)   ┌─────────────────┐
│ 브라우저  │ ───────────────────→ │ 우리 앱 (SPA)     │
│          │  ② 포탈 팝업으로 이동 │  index.html 부트  │
│          │ ←─────────────────── │  스트랩 IIFE      │
│          │                      └─────────────────┘
│          │  ③ 포탈 로그인
│          │ ───────────────────→ ┌─────────────────┐
│          │  ④ /?token=<JWT> 로  │ IBK AiPortal     │
│          │     리디렉트          │ (토큰 발급자)     │
│          │ ←─────────────────── └─────────────────┘
│          │  ⑤ POST /api/auth/sso { token }
│          │ ───────────────────→ ┌─────────────────┐
│          │  ⑥ 검증 → 사용자      │ 우리 API 서버    │
│          │     배치 → 로그인응답  │ (토큰 검증만 함)  │
└──────────┘ ←─────────────────── └─────────────────┘
```

**대원칙 3가지:**

1. **우리는 토큰을 발급하지 않고 검증만 한다.** 포탈이 서명한 JWT 를 HMAC 으로 재검증할 뿐이다.
   우리 서버가 같은 키로 토큰을 만들면 포탈 토큰 위조 통로가 된다 — 절대 금지.
2. **payload 는 암호화가 아니라 base64 인코딩일 뿐이다.** 서버의 검증 함수를 통과한 클레임만 사용한다.
   클라이언트가 보낸 payload 를 그대로 믿는 코드는 그 자체로 인증 우회다.
3. **신원의 진실은 사번(claim `id`) 하나다.** 이름·조직은 표시/보조 정보이고,
   조직의 진실은 HR 데이터다(토큰의 orgCd/orgNm 은 폴백으로만 쓴다 — §8 참고).

---

## 2. 사전 준비 (포탈 측 협의 사항)

새 시스템을 만들 때 포탈 운영팀과 협의해야 하는 것:

| 항목 | 내용 |
|---|---|
| 인증 팝업 경로 | 시스템마다 포탈에 등록된 진입 경로가 있다. weekly-ai 는 `…/portal/etc/weekly/popup` — 새 시스템은 자기 경로를 새로 등록받아야 한다 |
| 리디렉트 대상 | 포탈이 인증 후 `https://<새시스템>/?token=<JWT>` 로 보내도록 등록 |
| 서명 키(sign 문자열) | 토큰 검증에 필요. **코드·문서에 하드코딩하지 말고 env(`SSO_JWT_SIGN`)로 주입** — 값은 기존 시스템 운영 env 또는 포탈 운영팀에서 수령 |

---

## 3. 토큰 스펙 (실토큰 대조로 확정된 사실)

실토큰 3건 대조로 확인된 스펙이다. 문서와 실제가 다를 수 있으니 새 시스템에서도 실토큰으로 재확인할 것.

- **알고리즘**: HS256 (서명 32바이트 = base64url 43자). HS512 아님.
- **HMAC 키 유도**: `sign` 문자열의 **앞 32자를 base64 디코딩한 24바이트**.
  - 전체를 디코딩하거나 UTF-8 바이트를 그대로 쓰면 서명이 맞지 않는다 — 가장 많이 틀리는 지점.
- **유효기간**: 8시간 (`exp`). `iat` 도 같이 온다.
- **클레임**:

| 클레임 | 값 | 사용 |
|---|---|---|
| `id` | 사번 6자리 (예: `045345`) | **신원 식별자. 이것만 믿는다** |
| `name` | 이름 | 표시용 |
| `sub` | `"AiPortal Token"` 고정 문자열 | **식별자로 쓰면 안 됨** (전 직원이 같은 값) |
| `orgCd` / `orgNm` | 조직 코드/이름 | 폴백 전용 (§8) |
| `loginType` | `"SSO"` | 사용 안 함 |
| `role` 등 | 포탈 권한 | **사용 금지** — 포탈 권한이지 우리 시스템 역할이 아니다 |

---

## 4. 클라이언트 부트스트랩 (index.html)

Vue/React 마운트 **전에** 실행되는 순수 JS IIFE 로 넣는다(앱 번들에 넣으면 앱 로딩 실패 시 인증도 못 간다).
weekly-ai 의 `index.html` IIFE 를 그대로 복사해서 상수만 바꾸면 된다. 핵심 로직:

```
토큰 수신 우선순위:  URL ?token=  →  sessionStorage  →  (없으면) 포탈로 이동
```

반드시 유지해야 하는 장치들:

1. **URL 에서 토큰 즉시 제거** — `url.searchParams.delete('token')` + `history.replaceState`.
   주소창·히스토리·북마크에 토큰이 남으면 그 URL 하나로 그 직원이 된다.
2. **무한루프 가드** — 포탈로 보낸 시각을 sessionStorage 에 기록하고, 15초 안에 토큰 없이
   되돌아오면 다시 보내지 않는다. 없으면 포탈 세션 만료 시 두 사이트를 영원히 왕복한다.
3. **호스트 분기** — `localhost/127.0.0.1` → 로컬 목업(`/popup.html`), 호스트에 `devai` 포함 →
   개발 포탈, 그 외 → 운영 포탈. 새 시스템은 자기 팝업 경로로 상수만 교체.
4. **토큰이 없으면 저장된 세션이 있어도 무조건 포탈로 보낸다** — 포탈에 이미 로그인돼 있으면
   왕복이 눈에 안 띄고, 부서/팀/직책 변경이 새 토큰으로 즉시 반영된다.
5. **보관은 sessionStorage** (`weeklyAiSsoToken` 형식의 앱별 키) — 탭을 닫으면 사라진다.
   localStorage 에 넣으면 공용 PC 에서 다음 사람이 이어받는다.

토큰 접근은 **모듈 하나로만** 한다(weekly-ai 의 `src/services/ssoToken.ts` 참고).
인증 로직과 API 호출부가 각자 sessionStorage 를 읽으면 한쪽만 바뀌어 관리자 API 만 조용히 401 나는 사고가 났다.

---

## 5. 서버 검증 모듈 (그대로 복사해서 쓸 것)

`server/ssoAuth.js` 는 **외부 의존성 0**(node 내장 crypto)이고 폐쇄망 제약을 전제로 작성됐다.
새 시스템에 파일째 복사하는 것을 권장한다. 반드시 유지해야 하는 검증 순서:

```js
// 1) 헤더 알고리즘 고정 — alg:none / HS512 치환 공격 차단
if (header.alg !== 'HS256') throw ...;

// 2) 서명 재계산 + 타이밍 세이프 비교 (길이 먼저 확인 — timingSafeEqual 은 길이 다르면 throw)
const expected = crypto.createHmac('sha256', KEY).update(`${headerSeg}.${payloadSeg}`).digest();
if (actual.length !== expected.length || !crypto.timingSafeEqual(actual, expected)) throw ...;

// 3) 만료 검증 — exp 없는 토큰은 영구 토큰이 되므로 "거부"한다 (통과가 아니라)
//    시계 오차 허용(기본 60초, SSO_CLOCK_SKEW_SECONDS)과 nbf 도 본다.

// 4) 식별자는 claim `id`. 비어 있으면 거부. name 도 필수.
```

- 키 유도: `Buffer.from(SIGN.slice(0, 32), 'base64')` → 24바이트. 길이가 24가 아니면 기동 시 경고.
- 반환은 **소비하는 클레임만**: `{ employeeNo, name, orgCd, orgNm }`. 포탈의 role/otp 등은 버린다.
- 에러는 코드로 구분해 던진다(`SSO_TOKEN_MALFORMED / INVALID / EXPIRED`) — 클라이언트 폐기 판정(§7)이 이걸 쓴다.

---

## 6. 로그인 엔드포인트 패턴

```
POST /api/auth/sso  { token }
  → verifySsoToken(token)                    // 실패 시 401 (여기서 끝)
  → 사번 형식 검사 (우리 스키마 제약, 예: 6자리)
  → 사용자 배치: ① 사번 매칭 → ② (필요 시) 백필 → ③ 신규 생성
  → 로그인 응답 { user, userKey, ... }       // 수기 로그인이 있다면 같은 shape 으로
```

- **성공/실패를 서버 로그 한 줄씩 남긴다.** SSO 실패 시 브라우저에는 401 만 보이므로
  파드 로그가 유일한 관측 지점이다. 단, 로그에 내부 키(users.id 등)만 찍으면 사람을 알아볼 수
  없다 — 조직|이름을 직접 적는다.
- 사용자 자동 생성(provisioning)을 할 거면 동시 로그인 경합(unique 충돌 23505)을 409 로 번역한다.

## 7. 보호 API 인증 패턴 + 토큰 폐기 규칙

권한이 필요한 API(관리자 등)는 **호출마다** `Authorization: Bearer <SSO토큰>` 을 다시 검증한다.
로그인 응답의 isAdmin 같은 플래그를 믿지 않는다 — 판정의 진실은 항상 서버+DB 다.

**클라이언트의 토큰 폐기 기준 (운영 사고로 확정된 규칙):**

> 토큰을 폐기하는 것은 **서버가 그 토큰을 거절했을 때(401/400)뿐**이다.

- 타임아웃·네트워크 오류로 폐기하면, 서버가 잠깐 느린 것만으로 몇 시간 남은 멀쩡한 토큰이
  버려진다. 그 뒤 세션 복원으로 화면은 살아나므로 "로그인은 됐는데 특정 화면만 텅 빈" —
  원인이 어디에도 안 드러나는 최악의 상태가 된다.
- 5xx 도 폐기하지 않는다(토큰 문제가 아니라 서버 문제).
- 그래서 API 래퍼가 **"서버가 거절"과 "서버에 못 닿음"을 반드시 구분**해 줘야 한다
  (weekly-ai `api.ts` 의 `error.status` 유무 + `isNetworkError` 패턴).

## 8. 조직 정보 취급 (토큰 orgCd/orgNm)

**조직의 진실은 HR 데이터다. 토큰의 orgCd/orgNm 은 HR 에 그 사번이 아예 없을 때만 쓴다.**

일부만 토큰이 이기게 하면 같은 컬럼에 규칙이 다른 writer 가 둘 생긴다 — 로그인은 토큰 값,
배치는 HR 값. 어긋나는 순간 로그인 ↔ 배치마다 값이 왕복하며 화면 명단에서 사람이 사라지는
사고가 났다. 새 시스템이 HR 연동 없이 시작한다면 토큰 조직을 써도 되지만, HR 연동을 붙이는
순간 이 규칙으로 일원화할 것.

## 9. 로컬 개발 목업 (popup.html 패턴)

localhost 에서는 포탈에 갈 수 없으므로 목업 팝업을 쓴다:

- `public/popup.html` — 직원 선택 버튼 몇 개, 클릭하면 `/?token=<미리 서명한 JWT>` 로 이동.
  목업 토큰은 실제 sign 키로 서명하고 만료를 먼 미래로 둔 것이다.
- **⚠ 이 파일은 절대 운영 산출물에 포함되면 안 된다.** 유효한 토큰이 정적 파일로 노출되면
  누구나 그 직원으로 로그인할 수 있다. weekly-ai 는 vite 빌드 플러그인으로 `dist/popup.html` 을
  **빌드 시 자동 삭제**한다(`vite.config.mjs` 의 `stripDevOnlyAssets`) — 새 시스템도 같은 장치를 둘 것.
  사람이 지우기로 한 규칙은 언젠가 잊힌다.

## 10. 새 시스템 적용 체크리스트

- [ ] 포탈에 팝업 경로·리디렉트 URL 등록 (§2)
- [ ] `SSO_JWT_SIGN` env 주입 (하드코딩 금지) + 실토큰으로 검증 확인
- [ ] `server/ssoAuth.js` 복사 (검증 순서 4단계 유지)
- [ ] `index.html` 부트스트랩 IIFE 복사 (토큰 URL 제거 · 무한루프 가드 · 호스트 분기)
- [ ] 토큰 접근 모듈 단일화 (`ssoToken.ts` 패턴)
- [ ] `POST /api/auth/sso` + 서버 로그 한 줄 (§6)
- [ ] 보호 API 는 호출마다 Bearer 재검증 (§7)
- [ ] 클라이언트 폐기 기준: 401/400 만 (§7) — API 래퍼가 네트워크/거절 구분 제공
- [ ] `popup.html` 목업 + 빌드 시 자동 제거 장치 (§9)
- [ ] (HR 연동 시) 조직 정보 일원화 규칙 (§8)

## 11. env 정리

| 변수 | 용도 | 비고 |
|---|---|---|
| `SSO_JWT_SIGN` | 서명 키(sign 문자열) | 없으면 검증 전부 실패. 기동 시 키 길이(24바이트) 검사 |
| `SSO_CLOCK_SKEW_SECONDS` | 포탈과의 시계 오차 허용 | 기본 60초 |
| `SSO_DISABLE_HR_SCHEDULE` | (weekly-ai 전용) HR 스케줄 끄기 | 새 시스템은 해당 없음 |

## 12. 참조 구현 위치 (weekly-ai)

| 역할 | 파일 |
|---|---|
| 서버 검증 모듈 | `server/ssoAuth.js` |
| 로그인 핸들러 | `server/index.js` `handleSsoLogin` |
| 보호 API 게이트 | `server/index.js` `requireAdmin` |
| 클라이언트 부트스트랩 | `index.html` IIFE |
| 토큰 접근 모듈 | `src/services/ssoToken.ts` |
| 클라이언트 로그인/폐기 규칙 | `src/methods/auth.js` `ssoLogin` |
| API 래퍼(거절/네트워크 구분) | `src/services/api.ts` `request` |
| 로컬 목업 + 빌드 제거 | `public/popup.html`, `vite.config.mjs` `stripDevOnlyAssets` |
